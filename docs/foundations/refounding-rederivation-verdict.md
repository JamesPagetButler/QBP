# Refounding Re-Derivation Verdict — APC / HBH / IPH / f(0)

**Author:** qbp-oppenheimer (strategic lead) · **Date:** 2026-06-11
**Requested by:** qbp-architecture (bridge seq=601) — the "predictive worlds" map for the ontology charrette
**Status:** REVIEWED — Red Team + Gemini both APPROVE-WITH-CONCERN, all findings applied (§5); beekeeper-surfaced; routed to charrette 2026-06-11
**Method:** each archived Draft-v0.1 hypothesis decomposed into three layers, each layer bucketed against `paper/QBP-Foundations-v0_1.md` + the #474 operations-complete matrix. Four parallel re-derivation passes (one per hypothesis), then synthesis.

---

## 0. The headline finding (read this first)

**The #474 matrix FORCES the algebraic spine of every hypothesis but FOUNDS the bridge of none.**

Every archived hypothesis decomposes the *same* way:

```
  ┌─ ALGEBRAIC SPINE ──── now a zero-sorry kernel theorem (the matrix forces it)
  │
HYP ─ CRYSTALLISATION BRIDGE ── holography + ℍ→𝕆→ℍ projection — ABSENT from v0.1;
  │                              this is one shared, unproven primitive
  │
  └─ PHYSICAL PREDICTION ─ data-decidable; a few are bridge-LIGHT (the
                           maximally-discriminating ones)
```

So "which archived hypotheses survive re-derivation on v0.1?" has a single structural answer: **their floors are now solid, their ceilings all hang from one missing hook.** That hook — the **crystallisation bridge** — is not in v0.1 (verified: zero holographic/crystallisation/black-hole/spectral primitives in the foundation). It is itself the next fork to either *found* or *kill*; until then, none of APC/HBH/IPH is "derived from v0.1," and none is refuted by it.

**Caveat the verdict must carry (Red Team #4 / Gemini):** a *forced* spine makes the *unfounded* bridge dangerously seductive — "the math is so perfect, the physics must follow." The matrix hardens the floor; it lends **zero** support to the ceiling. Treat every algebra↔physics mapping below as unearned until the bridge is built.

This directly answers the charrette's discrimination question: the **bridge-light predictions** (APC's EXP-10 Test C; IPH's fixed-T entanglement-diffusion) are the maximally-discriminating tests, because they need only the Layer-1 FORCED facts (the DOF ladder), not the unproven bridge.

---

## 1. Verdict table

| Hypothesis | Spine (vs #474) | Bridge | Predictions | **Bucket** |
|---|---|---|---|---|
| **APC** Algebraic Particle Classification | **FORCED** — Hurwitz termination, the loss-ladder, DOF doubling all kernel-proven; APC was if anything algebraically *timid* | crystallisation (absent) | EXP-10 Test C is **bridge-light** + data-decidable | **SPLIT** — forced spine · open bridge · 1 live empirical wedge. *Nothing killed.* |
| **HBH** Holographic Boundary | **PARTIAL** — 𝕆 non-assoc/alternative/last-division-algebra + 7 Fano *lines* forced; but the 7-subalgebra **count**, **G₂ dynamics**, ℍ→𝕆→ℍ **process** NOT forced (asserted in comments / a `log 7 = log 7` tautology) | **IS** the crystallisation conjecture | only low-power CMB anomaly re-attribution; sharpest test (sequestration vs compactification) null-in-practice | **PRESERVED-OPEN but discrimination-poor** — no feasible near-term test separates it from ΛCDM |
| **IPH** Information Propagation | **KILLED as derivation** — "time = associativity" is an unfalsifiable overlay on the associator theorem; HBH's own §5 Q6 lists the arrow⟺associativity theorem as *still open* | inherits HBH's Γ/boundary (unfounded) | preserved-open; best = fixed-T entanglement-diffusion (P3) | **SPLIT** — spine killed-as-derivation · bridge unfounded · predictions live. *Collapses if HBH bridge unfounded.* |
| **f(0)** NCG spectral coupling | **N/A** — orthogonal NCG track; **zero** connection to the v0.1 CD spine | — | δ₁≈δ₃ (9.4%), c=π/8 (open task A1) | **MOSTLY KILLED + 1 OPEN** — 1-loop bootstrap killed (Σbᵢ=6.1≠0); coincidences are numerology/unverified; only live thread = prove c=π/8 from trace invariants |

---

## 2. Per-hypothesis detail

### 2.1 APC — SPLIT (strongest survivor)
- **Spine FORCED:** "only ℝℂℍ𝕆 are normed division algebras," "Hurwitz terminates at 𝕆 because 𝕊 has zero divisors," the order→comm→assoc loss-ladder, ℝ1/ℂ2/ℍ4/𝕆8 DOF doubling — all now zero-sorry theorems (#474 AC1/AC4/AC5, Hurwitz boundary). The matrix refutes **none** of APC's algebra and even sharpens it (the 𝕆-alternative / 𝕊-non-alternative boundary APC never used).
- **Bridge OPEN:** the master claim "each algebra level *manifests as* a bulk particle class via the holographic boundary," plus mass-as-angle, Higgs-as-ℝ→ℍ-promotion, generations-from-axes, colour-from-subplanes — all rest on the crystallisation conjecture (absent from v0.1).
- **One bridge-light wedge:** EXP-10 Test C (algebraic-vs-mass fidelity-asymmetry cross-comparison). Standard QM / decoherence / semiclassical gravity all predict **zero** asymmetry in both configs; APC predicts a specific ranking depending only on the DOF count (Layer-1 FORCED). Cheapest first move: the **Test C literature review** (STEP P1, zero cost).
- **Derivation-artifact risks flagged:** Higgs-as-"ℝ→ℍ promotion" is DOF bookkeeping (2+2=4), not a mechanism; "photon can't be promoted" is post-hoc; Pred #8 ("no stable 𝕆-particles ever") is near-unfalsifiable by construction.

### 2.2 HBH — PRESERVED-OPEN, discrimination-poor
- **Spine:** 𝕆 non-associative (associator coeff 2≠0, kernel-checked), alternative, last division algebra, 7 Fano lines — FORCED. **But** the two rows the cosmology actually needs are NOT forced: the *count* "exactly 7 quaternionic subalgebras" is asserted in a comment (`FanoChoiceInformation.lean:21`), and `Aut(𝕆) ≅ G₂` has **no theorem in the repo** (only numeric coincidences like 56=14×4). The "crystallisation threshold = ln 7" is injected by *defining* it, and `fano_choice_information` proves only `log 7 = log 7`.
- **Bridge:** HBH *is* the crystallisation conjecture in cosmological costume. The ℍ→𝕆→ℍ′ *process*, the holographic projection, and the subalgebra-selection→gauge-group map are all absent from v0.1.
- **Predictions:** five of six route through CMB anomaly re-attribution or undelivered Fano/G₂ templates — low-power, post-hoc, cosmic-variance-limited. The one qualitatively sharp test (extra dimensions in flat spacetime vs only in strong gravity) is null-in-practice. **No feasible test currently separates HBH from ΛCDM.**
- **Two prerequisite theorems worth filing regardless** (standard math, currently comment-asserted): (i) `Aut(𝕆) ≅ G₂`; (ii) "𝕆 has exactly 7 four-dimensional associative subalgebras, in G₂-orbit, indexed by the Fano lines."
- **Severity sharpened (Gemini):** because `Aut(𝕆)≅G₂` isn't even in Lean, HBH doesn't merely lack a *physical* bridge — it lacks part of its *mathematical* spine. The G₂ dynamics the cosmology leans on are unproven at the algebra level, not just unmapped to physics.
- *(Provenance note, Red Team #3: the `log 7 = log 7` tautology and the `FanoChoiceInformation.lean:21` comment-cite are agent-verified at file level, not re-audited line-by-line in this synthesis.)*

### 2.3 IPH — SPLIT, spine killed-as-derivation
- **Spine KILLED (as a derivation):** "the arrow of time is a consequence of associativity" is an interpretive overlay — #474 proves an associator coordinate is nonzero, nothing about events/order/causality/time. HBH's own §5 Q6 concedes the "arrow ⟺ associativity" theorem **does not yet exist** (and cites an external lead, García-Pintos). Presented in the register of a derivation; actually unfalsifiable as stated.
- **Bridge unfounded:** Γ ("accumulated boundary computation steps"), the boundary Σ, FDT-as-identity — all inherited from HBH, none in v0.1.
- **Predictions preserved-open:** best discriminator is **P3** — entanglement-structured diffusion at *fixed kinetic temperature* (isolates the novel ε_info term; removes the gravity term that merely re-derives GR; near-term feasible). Gated on an **a-priori operational definition of Γ**, without which the prediction layer degrades to the reparameterisation/curve-fitting trap the doc's own Red Team note flags.
- **Dependency:** IPH's spine+bridge *collapse* if HBH's bridge is unfounded (Γ has no referent); the predictions survive only as bare phenomenology, not as predictions *of a theory*.

### 2.4 f(0) — orthogonal NCG track, mostly killed
- **Not on the spine:** f(0) is the Chamseddine–Connes spectral-action coupling on the finite algebra ℂ⊕ℍ⊕M₃(ℂ) — imported wholesale, **zero** connection to the v0.1 CD tower (grep-verified).
- **KILLED:** the 1-loop heat-kernel bootstrap (the only genuine derivation attempt) fails — Σbᵢ = −4.1+3.2+7.0 = 6.1 ≠ 0; both docs concede it. The "f(0)=2·(1/α_unif)" and six-Hessian "confluences" are retracted by the investigation report itself as "partially circular… a coincidence of notation."
- **PRESERVED-OPEN (one thread):** prove-or-disprove **c = π/8** from the SM fermion trace invariants on ℂ⊕ℍ⊕M₃(ℂ) (open task A1) — a finite, decidable computation. The only path that converts f(0) from fitted-input to derived.
- **⚠️ PROVENANCE GAP (new finding):** the headline accuracy figures do **not reproduce** in the committed artifacts — `f0-results.json` with f0=83.51 yields 1/α_em = 82.5 (vs measured 137), sin²θ_W = 0.42 (vs 0.23), α_s = 0.027 (vs 0.118). The "+0.08%" / "computed 0.391" numbers live only in prose; no committed script computes a trace invariant ≈ π/8. **The single-anchor weakness the vulnerability assessment was commissioned to remove was not removed.** Treat the investigation report (honest) as superseding the vulnerability assessment (overclaims).

---

## 3. What this means for the fork-lattice / charrette

1. **The algebraic fork-lattice is given, not open** — #474 already maps forced-vs-forkable for the *division-algebra* axioms (the ✓ rows forced, the ✗-transitions the forkable loss-ladder). The charrette can treat it as closed and spend its discrimination budget above the spine.
2. **The crystallisation bridge is the one shared fork** every physical hypothesis depends on. Founding it (a "crystallisation contraction" `c:[0,1]→CDStruct(ℝ⁸)` + a 𝕆→ℍ_L selection map + a G₂-orbit on the 7 lines) or killing it is the highest-leverage next theory move — it would simultaneously found/kill APC's bridge, HBH wholesale, and IPH's Γ. **Founding requirement (Gemini review):** a static projection 𝕆→ℍ is not enough — the bridge must take the form of a **dynamical mechanism (an action principle / Lagrangian or Hamiltonian)** that makes the octonionic DOF energetically or entropically inaccessible. Without an action that *penalises* the 𝕆 state, the contraction is asserted, not derived. This is the bar any bridge-founding attempt must clear.
3. **Discrimination bottleneck confirmed:** the only bridge-light tests are **APC EXP-10 Test C** and **IPH fixed-T entanglement-diffusion** — these are the maximally-discriminating experiments, consistent with Test C / EXP-11 already being the critical path.
4. **Logic gate already doing work:** this re-derivation *is* Gate-1 — it killed IPH's spine-as-derivation and f(0)'s bootstrap, and exposed an f(0) provenance gap, before any sim cycle.

---

## 4. Recommendations (verdict-only; survivors stay archived per beekeeper 2026-06-11)

- **APC** — keep archived; fund the **Test C literature review** (zero cost, bridge-light, most distinctive prediction).
- **HBH** — keep archived; do **not** invest in CMB-discrimination (low power). If anyone touches it, the two prerequisite theorems (Aut(𝕆)≅G₂; exactly-7-subalgebras) are the only kernel-worthy pieces.
- **IPH** — keep archived; its predictions are live only with an a-priori Γ definition, which is itself gated on the bridge.
- **f(0)** — keep archived as a **separate NCG track** (not v0.1 spine); the one decidable move is computing c=π/8; first reconstruct the missing "0.391" computation (agent-reported absent; not re-audited exhaustively here). Provenance gap filed as **#535**.
- **Cross-cutting** — the crystallisation bridge is the next fork worth a dedicated scope (found-or-kill). Any founding attempt **must** supply an action principle (above), not a static projection. Not opened here; flagged for the charrette.

---

## 5. Review record

| Stage | Reviewer | Verdict | Disposition |
|---|---|---|---|
| Re-derivation | 4 parallel skeptical passes (one per hypothesis) | — | raw material |
| Synthesis verification | qbp-oppenheimer | — | f(0) numbers re-ran (82.52 ✓), no Aut(𝕆)≅G₂ theorem ✓, v0.1 holographic-primitive count 0 ✓ |
| Red Team | Sabine / Grothendieck / Knuth | **APPROVE-WITH-CONCERN** | 4 calibration findings — all applied (bridge-light; killed-as-derivation qualifier; agent-sourced cites marked; seductive-floor caveat) |
| Gemini (`gemini-3-pro-preview`, real API) | Furey / Feynman | **APPROVE-WITH-CONCERN** | agreed with Red Team; added 2 — action-principle requirement + HBH-lacks-mathematical-spine — both applied |

No reviewer conflict. No claim retracted. Status: **reviewed, calibrated, ready to route to the charrette.**
