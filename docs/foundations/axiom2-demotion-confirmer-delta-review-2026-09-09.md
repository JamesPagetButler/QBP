# AXIOM-2 demotion — Red Team confirmer, DELTA pass on package v0.3 §9 (rounds 5–6)

Scope: §9 (+ §8 items 5–6), transcript rounds 5–6, verbatim turns 56–59, `inflight_measure_check.py` (re-run: all assertions pass — rank 8 at 20 vacua; 20 000 samples, none with V < 10⁻³; V min 0.030 / median 0.683 / max 0.9999), ledger v5_3.v0.3 anchors, horn-1 ruling (§7), `CrystalHosting.vacuum_iff_parametrised`. Round-4 review stands unchanged. Branch `research/axiom2-demotion` @ 79b1d20.

## Findings

| # | Finding | Where | Sev | Persona |
|---|---|---|---|---|
| 1 | **§9c's derivation is circular as used in D2.** Horn 1 (§7 ruling) is a measure on S¹⁴ ⊂ Im𝕊 — the *sedenion* state sphere. D2 uses POST-hosting to supply "level ≥ 16" (§2a: "crystallisation ⇒ ≥ 16"; META-2 supplies "= 16"). Deriving POST-hosting from a measure that already lives on the level-16 sphere assumes the lower bound. Run the same argument one level down: level-generic horn 1 at 𝕆 is the surface measure on S⁶ ⊂ Im𝕆, every imaginary octonion is a crystal (`assoc_diag_left`, alternativity), and the universe *is* born crystallised — Gemini's kill fires at 8. So what §9c proves is: under level-generic horn 1, "a.s. histories start in flight" ⇔ "V ≢ 0 at the substrate level" ⇔ "substrate level ≥ 16". POST-hosting is *re-expressed* as the lower bound, not derived from the ruling. | §9c "Consequence, if a second party confirms"; §2a | 🔴 | Grothendieck |
| 2 | **"Every" is false; "almost every" is what is derived.** Vacua exist and are Lean-named (`ell_isVacuum`, `sAll_isVacuum`); V = 0 is the global minimum so they are fixed points of any V-descending rule. A history starting at a vacuum never passes through V > 0. The history form says "every universe's history *must* pass through" — contradicted on a measure-zero set of states the Lean itself exhibits. Derived statement: "with probability 1 under any initial measure absolutely continuous on S¹⁴". Either the wording becomes "almost every" (then it is a consequence, subject to #1) or it stays "every" (then it is a postulate, and a false one on the vacuum locus). | §9c table row 1; turn 59 §3 | 🔴 | Grothendieck |
| 3 | **The history form bundles an owed claim.** "…to reach a crystallised state" needs the rule's flow to converge to 𝓤 — hosting §4(c), numerical only, Łojasiewicz owed. D2 needs only the first half ("non-crystal states are physically realised"); the second half is the rule's (#635), which the §7 ruling itself calls "the weakest link". §9c's root list (AXIOM-1; META-2; horn 1; crystal definition; CD tower) omits the rule and the state-space identification (state = unit imaginary sedenion; Prop 16 layer 3). Split the sentence: (i) a.s. starts in flight — derived given #1's caveat; (ii) reaches 𝓤 — owed. | §9c; hosting doc §4(c); §7 ruling | 🟡 | Sabine |
| 4 | **Dimension count and measure zero: correct, and provable more cheaply.** Image = {(αu, b₀ + γu)} = {a ∥ Im b}: 7 + 1 + 1 = 9 in Im𝕊, 8 on S¹⁴, codim 6. The 2:1 identification (u, α, γ) ~ (−u, −α, −γ) and the pole stratum (α = γ = 0, S⁶ collapsed) change nothing — finite cover, lower-dim stratum. Script's rank-8 Jacobian confirms. But the count is not needed: V is a polynomial on ℝ¹⁶, not identically zero on S¹⁴ (`inFlight_nonempty`, `sedWitX_not_isVacuum`), and the zero set of a non-trivial real-analytic function on a connected manifold has measure zero. Every input to that lemma is on master. State the lemma that way; the codim-6 fact is a bonus. Nit: the §7 ruling says "O(16)-invariant … on S¹⁴" (S¹⁴ ⊂ ℝ¹⁵ is O(15)-invariant); if the ensemble were on S¹⁵ the vacuum locus is codim 7 — conclusion unchanged. | §9c; script; §7 | 🟢 | Grothendieck |
| 5 | **Horn 1 is the surface measure (yes), and it is far more than needed.** §7: "the volume form of N's metric, the unique O(16)-invariant probability measure on S¹⁴" — absolutely continuous, not singular. But the conclusion uses *only* absolute continuity: Q_μ (μ ≠ 1) and the thermal family (β ≠ 0) — the ruling's excluded hypotheses — give the same a.s. result. Good for robustness (reversing the ruling to any non-singular alternative changes nothing); fatal for the "matched pair" line: MaxEnt does no work here, absolute continuity does, and horn 1 was ruled for a fixed algebra while META-2 ranges over levels. "Saturation for levels, MaxEnt for states" is rhetoric, not a structural statement. | §9c last two sentences; §2a "Why MaxEnt alone fails" | 🟡 | Sabine |
| 6 | **"Compositions of isometric R_t are ergodic on the state sphere" is false as stated.** For t ∈ 𝕆 ∪ 𝕆ℓ the map x ↦ x·t either preserves ‖cdLo x‖², ‖cdHi x‖² separately (t ∈ 𝕆) or swaps them (t ∈ 𝕆ℓ) — doubling formula; verified numerically (200 samples, deviation 1.4×10⁻¹⁴). The group they generate lies in (O(8) × O(8)) ⋊ ℤ/2 and preserves |‖a‖² − ‖b‖²|: not transitive on S¹⁵, let alone ergodic. (R_t also does not preserve Im𝕊, so "on S¹⁴" is the wrong sphere.) The refutation of the arrow rooting does *not* need this sentence — "volume-preserving dynamics + coarse-graining suffices for a statistical arrow" (Boltzmann) is enough — so strike it. Gemini "agreed unconditionally" with a false auxiliary claim: gate-3 miss. | §9a row 4; transcript R5; turn 57 §A | 🟡 | Grothendieck |
| 7 | **DERIV-arrow was quoted for one of its two clauses.** Full statement: "Information preservation + lossy projection implies irreversibility in the bulk. **Gamma counter is monotonic.**" The second clause is a *fundamental* arrow — Γ counts crystallisation steps (DERIV-crystallisation-asymptotic: "time IS the crystallisation"), i.e. the non-isometric transient. So "the ledger's own arrow is projective" is half the anchor. Consequence: the arrow rooting does not "fail completely"; the *thermodynamic* arrow cannot root POST-hosting, and the *Γ-arrow* rooting collapses into §9b's rank-1 α-drift route — which by §9c tests the local claim only. Say that; it is the same conclusion with the anchor read whole. PRED-revival-exact is used fairly. | §9a; ledger DERIV-arrow | 🟡 | Sabine |
| 8 | **§9b's α "category mismatch" contradicts the ledger it cites.** MEAS-alpha's prediction is labelled "1/α_em at M_Z" = 128.05 vs 127.95 ± 0.02 (PDG 2024): that is a **5σ** discrepancy at the ledger's own comparison scale, recorded as `discrepancy_pct 0.08` with status `coherent`. CHAIN-axioms-to-alpha (steps "Axioms → gauge groups → f(0) → RGE → α") notes "weakest link is f(0); **all other steps proven or standard physics**" — the chain claims RGE is done, and blames INST-f0. Gemini's turn-57 B2 re-attributes the 0.08% to "uncalculated RGE running", the driver adopted it verbatim, nobody opened the chain. Either the chain note is wrong (RGE not done; then MEAS-alpha's M_Z label is wrong) or §9b is. Also: a_e determines α(0), not α(M_Z); the running to M_Z is standard QED + hadronic VP with uncertainty ≈ ±0.02 in 1/α — the ppb precision of a_e never reaches M_Z. "7 orders of magnitude" is the wrong axis; the relevant precision is MEAS-alpha's own ±0.02, already violated. | §9b row 2; ledger MEAS-alpha, CHAIN-axioms-to-alpha; turn 57 §B2 | 🔴 | Sabine / Knuth |
| 9 | **"QBP predicts a null for g-2" is absence-of-mechanism, not a prediction.** DERIV-observation ("EM observation accesses the ℂ subalgebra of ℍ") and DERIV-sedenion ("zero divisors at inter-cell boundaries") contain no computation bounding seam contributions to a QED vertex. And a strict null is not even what the ledger says: DERIV-crystallisation-asymptotic has α drifting, so a_e = α/2π + … drifts with it at an unpredicted rate. Honest wording: "no mechanism named; SM–experiment agreement bounds any unnamed seam term at ≲ 10⁻¹⁰ relative (this number is arithmetic on the sourced a_e uncertainty, and the true SM comparison is limited by the independent α input, not by a_e)". "Solidifies PRED-no-gup" is a loose association — PRED-no-gup is about ħ per Γ-step (chain: DERIV-constants), which a_e does not test. Sourced numbers otherwise used as sourced; no unmarked new numbers beyond the derived 10⁻¹⁰. | §9b row 1; turn 57 §B1 | 🟡 | Sabine |
| 10 | **Ranking defensible; "local not global" holds — with the empirical content stated.** α-drift is rank 1 by elimination (the only observable touching in-flight physics); it is untested until a rate exists; CONSTRAINT-gdot already records Ġ/G < 10⁻¹² as "constraint not detection". "α-drift tests DERIV-crystallisation-asymptotic, not POST-hosting" is right. Net: after §9, POST-hosting (history form) has *no* observable and its only kill is theory-internal; the split's entire empirical face is one unpredicted rate. The package should say this in §0, not leave it to be inferred. | §9b rows 3–4; §9c | 🟢/🟡 | Sabine |
| 11 | **Tell: "physically illiterate and mathematically unnecessary".** The chain was the driver's own, attacked by the driver in the same message; Gemini's thinking block reads "I will aggressively agree with the Red Team's takedown", then the reply escalates the driver's self-criticism and adds no re-derivation (and endorses the false ergodicity clause, #6). Turn 59: Gemini's kill was disposed of by a five-line dimension count it did not attempt. Fast-agreement pattern, both rounds. | turns 57, 59 | 🟡 | Knuth |
| 12 | **The post-yield claim was pressure-tested by no one.** Round 6 genuinely tested round 5's easy answer (good). Round 6's own easy answer — "kill decidable ⇒ POST-hosting not a root" — was made after Gemini yielded, checked only by the driver's script (which tests the measure, not the logic), and reaches the beekeeper flagged "awaiting second party". This review is that party: #1–#3 are the result. | §9c; §8 item 6 | 🟡 | Knuth |
| 13 | What holds: Gemini's kill ("born crystallised at Γ = 0 under horn 1") **cannot fire at 𝕊** — the vacuum locus is codim 6 and any absolutely continuous ensemble puts zero mass on it; the script is correct and re-run; the DERIV-arrow/PRED-revival-exact argument against a *fundamental thermodynamic* arrow is sound; POST-hosting's separation from DERIV-crystallisation-asymptotic is right; §8 items 5–6 are honest about status. | §9 | 🟢 | all |

## Gate §3, rounds 5–6

| Condition | Round 5 | Round 6 | Evidence |
|---|---|---|---|
| (1) next steps with reasons | met | met | §8 items 5–6 |
| (2) assumptions surfaced | partial | partial | unsurfaced: ergodicity (#6), RGE re-attribution (#8), every/a.e. (#2), circularity (#1) |
| (3) grounded | partial | partial | DERIV-arrow half-quoted (#7); CHAIN note and MEAS-alpha σ unread (#8); 10⁻¹⁰ derived, not sourced (#9) |
| (4) shared understanding | not certifiable | not certifiable | Gemini yielded twice; §9c claim post-yield (#12) |
| (5) easy answer tested | met (arrow chain, by the driver) | half: R5's answer tested; R6's own not (#12) | transcript |

## Verdict on the delta

**§9a — CONFIRMED-ON-CONDITIONS.** (C1) strike the ergodicity sentence (#6); (C2) quote DERIV-arrow whole and replace "fails completely" with "the thermodynamic arrow cannot root it; the Γ-arrow rooting is the α-drift route" (#7).

**§9b — CONFIRMED-ON-CONDITIONS.** (C3) rewrite the α row: MEAS-alpha is 5σ at M_Z on its own terms; CHAIN-axioms-to-alpha claims RGE is standard physics; resolve which is wrong before "category mismatch" is asserted (#8); (C4) "null" → "no mechanism named; bound on unnamed seam terms"; drop "solidifies PRED-no-gup" (#9); (C5) state in §0 that the split's only empirical face is an unpredicted rate (#10).

**§9c — NOT CONFIRMED as "POST-hosting is not a root."** Confirmed as a result: *the kill cannot fire at 𝕊* — dimension 8 in 14, measure zero under any absolutely continuous ensemble, script correct (#4, #5, #13). Not confirmed: the inference from that to "derived from horn 1 + Prop 15". It is circular (#1), overstated from "almost every" to "every" (#2), and bundles the owed convergence (#3). The most that may be told to the beekeeper: **"POST-hosting's Gemini-kill is dead; POST-hosting itself remains a root, now sharpened to exactly the level lower bound 'V ≢ 0 at the substrate' plus the rule's convergence."** The root count stays as v0.2 §2a (with the rule now visible as a fifth). "Not a root" may be stated only as a conjecture, and this reviewer's judgement is that it is false as stated. (C6) rewrite §9c accordingly; (C7) if kept as a conjecture, mark it so in §0 and §8 item 5, and add the level-8 counter-instance so the circularity is on the record.

— Red Team confirmer (delta) — Sabine · Grothendieck · Knuth
