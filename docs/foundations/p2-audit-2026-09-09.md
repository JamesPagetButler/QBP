# P2 versus P2′ — a rigor audit under "few roots and long chains" (v0.1, 2026-09-09)

**Status:** beekeeper-directed ("can we test whether P2 or P2′ is the more rigorous answer? — run the audit as a Gemini round"). Two rounds (session `debate-20260904-151140`, turns 62–65, verbatim beside this file), the second a pressure-test of the first. **No confirmer pass yet**; the structural verdict is the dyad's, the withdrawn claims are Gemini's and named. Nothing here edits the ledger or Lean; the two P2′ lemmas are stated, not yet proved. Written by qbp-oppenheimer.

**One line.** P2′ (the encoding octonion is the Cayley–Dickson cell) asserts strictly less than P2 (an octonion chosen around the crystal's ℍ) on every count the wisdom measures, and — once the premise that links geometry to observation is written down and counted for *both* readings — P2′ entails DERIV-observation while P2 contradicts it. The audit says which reading asserts less and which is consistent with the ledger; it does not say which is true. No experiment on record distinguishes them.

## 1. The two readings (objects on master)

A universe's hosted algebra is ℍ_s = span{1, ℓ, U, ℓU} (PROOF-crystal-hosts-quaternion). It contains ℓ = e₈, so it is not inside the Cayley–Dickson cell 𝕆 = span{e₀..e₇}; it meets the cell in exactly ℂ_u = span{1, U}, and ℍ_s = ℂ_u ⊕ ℂ_u·ℓ. The octonion subalgebras of 𝕊 that contain ℍ_s form (conjecturally all of them) the family 𝕆'_v = ℍ'_v ⊕ ℍ'_v ℓ, one per complex line in u^⊥, a ℂP² (boundary note, PR #643).

| | P2 | P2′ |
|---|---|---|
| AXIOM-2's encoding octonion | some 𝕆'_v ⊃ ℍ_s, chosen | the cell 𝕆, canonical |
| ℍ_s and the encoding | inside | meets it in ℂ_u; ℓ and ℓU lie across the cell boundary |
| "the gap" | ℍ_s^⊥ ∩ 𝕆'_v, 4-dim (DERIV-holographic literal) | ℂ_u^⊥ ∩ 𝕆, 6-dim (DERIV-holographic's sentence must change) |

## 2. The audit (both parties wrote theirs before comparing; the counts agreed exactly)

| Count | P2 | P2′ |
|---|---|---|
| New roots | 1 — the ℂP² point (field on `Universe`, later crystallisation stage, or observer choice) | 0 |
| Unproved links | 2 — 𝕆'_v is an octonion ⊃ ℍ_s (provable, moderate); completeness of the family (conjecture) | 0 |
| Provable-now links | 1 (the codimension count) | 2, trivial: ℍ_s ∩ 𝕆 = ℂ_u; ℍ_s = CD(ℂ_u) by ℓ |
| Constitutional edits forced | none beyond flag 3 | DERIV-holographic's "4D gap" sentence (already being rewritten under flag 3) |
| Fit | DERIV-holographic literal; DERIV-sedenion strained ("two copies of the encoding" means the cell) | DERIV-sedenion exact; DERIV-observation literal; DERIV-holographic's gap sentence changes |

## 3. The pressure-test (round 8) — what survived and what was withdrawn

| Round-7 claim (Gemini) | Test | Outcome |
|---|---|---|
| "P2′ *derives* DERIV-observation" | needs an unnamed premise **(O): what an observer can access is what is encoded in its own cell** — a physical reading of AXIOM-2's "encoding", not on record | **corrected**: (O) is a root and must be counted for both readings. **P2′ + O ⇒ DERIV-observation** (the cell sees ℂ_u). **P2 + O contradicts DERIV-observation** (the cell 𝕆'_v sees all of ℍ_s; a third root would be needed to suppress two directions). This is the audit's real discriminator — a *consistency* discriminator at the ledger level, not an experiment |
| "P2′ distinguishes chirality k = ℓU vs Uℓ" | ℓU = −Uℓ, same line, both in the high half; the sign is fixed by the doubling formula, not by the reading | **withdrawn**. P2′ fixes an asymmetry of *type* (U in-cell; ℓ, ℓU out) — an orientation of the pair (in, out) — not the sign of k. Chirality stays unforced under both |
| "predicts extreme anisotropy in spatial propagation" | no mechanism on record propagates anything along Im ℍ_s; the hosted physics is topological (Agda S³ H-space, charges) | **withdrawn** as a category error (Gemini's words) |
| The H-space product "crosses the boundary" (U·ℓ ∈ 𝕆ℓ) | ℍ_s is closed; products move between the halves of ℍ_s = ℂ_u ⊕ ℂ_u ℓ; the cell-component map x ↦ cdLo x is linear, **not** multiplicative ((Uℓ)·ℓ = U has cell component U, the components' product is 0) | **kept as a fact**; Gemini's gloss that this "is exactly what quantum measurement means / hidden variables / decoherence" is **interpretation with nothing on record behind it** and is not adopted here. What is on record: under P2′ + O the map from the observer's algebra to what its cell retains is linear and not a homomorphism — which is at least the *shape* DERIV-observation's "lossy projection" needs |

## 4. Audit with (O) counted

| Count | P2 + O | P2′ + O |
|---|---|---|
| New roots | 2 (ℂP² point; O) — and a 3rd to rescue DERIV-observation | 1 (O) |
| Consistency with DERIV-observation | **contradicts** | **entails** |
| Everything else | as §2 | as §2 |

## 5. Verdict, kill, and what the audit cannot do

**Verdict (structural):** P2′ is the more rigorous reading — fewer roots, no unproved links, and the only one consistent with DERIV-observation once the observation premise is stated. Cost: one sentence of DERIV-holographic, inside an edit already owed.

**Kill for P2′ + O:** an observable that requires an observer to access non-commuting information (more than ℂ_u) directly at the encoding without projection. None on record.

**What it cannot do:** say which reading is *true*. Both are consistent with every theorem; no experiment distinguishes them; DERIV-3plus1's "3 spatial directions = Im ℍ_s" under P2′ puts two of the three outside the cell — a *bookkeeping* fact today (the hosted physics is direction-blind topology), a physical claim only if a direction-dependent mechanism is ever written.

## 6. Next steps, with reasons

1. **Confirmer pass** (Red Team) on §3–§4 before this is put to the beekeeper as a result — *because* the discriminator rests on a premise (O) the dyad introduced in-round, and round 7 showed three over-claims.
2. **Two Lean lemmas** in `CrystalHosting.lean`: `quatSpan_inter_lowHalf` (ℍ_s ∩ 𝕆 = span{1, U}) and `quatSpan_eq_cd_double` (ℍ_s = ℂ_u ⊕ ℂ_u ℓ) — *because* they are the whole mathematical content of P2′ and are five lines each.
3. **Beekeeper ruling** on P2′, **together with** the flag-3 split and the three-name boundary vocabulary (encoding octonion / holographic gap / seam), as qbp-architecture asked on #643 — *because* the "4D gap" sentence is edited in both.
4. **Premise (O)** goes into the flag-3 draft as the observation clause of POST-observer-associativity, or as its own POST — *because* it is what links the boundary to measurement and was previously implicit.
