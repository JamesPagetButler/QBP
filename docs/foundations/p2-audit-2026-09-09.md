# P2 versus P2′ — a rigor audit under "few roots and long chains" (v0.2, 2026-09-09)

**Status:** beekeeper-directed ("can we test whether P2 or P2′ is the more rigorous answer? — run the audit as a Gemini round"). Two rounds (session `debate-20260904-151140`, turns 62–65, verbatim beside this file), the second a pressure-test of the first. **Confirmer (Red Team): CONFIRMED-ON-CONDITIONS** (`p2-audit-confirmer-review-2026-09-09.md`; six conditions, all applied in v0.2). The confirmer was the missing pressure-test of round 8 and it **changed the discriminator**: (1) "the cell is canonical" was load-bearing and **false** — an order-3 automorphism ρ of 𝕊 that *fixes ℓ* carries the Cayley–Dickson half to a different octonion subalgebra and preserves every crystal, so under P2′ "the cell" is a choice in a ℤ/3-torsor (one **discrete** root, verified: `analysis/473-dirac-probe/p2_cell_torsor_check.py`, asserted); (2) "P2 + O contradicts DERIV-observation" was an artefact of the unstated quantifier in (O) and is **withdrawn** — with (O) as an upper bound neither reading contradicts; P2′ + O *selects* the observed ℂ (up to the ℤ/3), P2 + O selects none. §7 tell recorded: both rounds were fast agreement (the sealed table sat in the same prompt; the discriminator was the driver's proposal returned verbatim); no party ever held the P2 side. Nothing here edits the ledger or Lean; the two P2′ lemmas are stated, not yet proved. Written by qbp-oppenheimer.

**One line.** P2′ (the encoding octonion is *a* Cayley–Dickson half — one of three, chosen) asserts less than P2 (an octonion chosen from a ℂP² around the crystal's ℍ): a discrete choice against a four-parameter one, no octonion-existence link, no completeness conjecture. Once the premise that links geometry to observation is written down with its quantifier and counted for *both* readings, P2′ + O **selects** the observed ℂ (ℂ_u, up to the ℤ/3) and P2 + O selects none; **neither contradicts** DERIV-observation. The audit says which reading asserts less; it does not say which is true, and its only discriminator is a selection made by a premise introduced in-round. No experiment on record distinguishes them.

## 1. The two readings (objects on master)

A universe's hosted algebra is ℍ_s = span{1, ℓ, U, ℓU} (PROOF-crystal-hosts-quaternion). It contains ℓ = e₈, so it is not inside the Cayley–Dickson cell 𝕆 = span{e₀..e₇}; it meets the cell in exactly ℂ_u = span{1, U}, and ℍ_s = ℂ_u ⊕ ℂ_u·ℓ. The octonion subalgebras of 𝕊 that contain ℍ_s form (conjecturally all of them) the family 𝕆'_v = ℍ'_v ⊕ ℍ'_v ℓ, one per complex line in u^⊥, a ℂP² (boundary note, PR #643).

| | P2 | P2′ |
|---|---|---|
| AXIOM-2's encoding octonion | some 𝕆'_v ⊃ ℍ_s, chosen from a ℂP² | a Cayley–Dickson half 𝕆 — **one of three** (the ℓ-fixing order-3 automorphism ρ permutes them; confirmer #1) |
| ℍ_s and the encoding | inside | meets it in ℂ_u; ℓ and ℓU lie across the cell boundary |
| "the gap" | ℍ_s^⊥ ∩ 𝕆'_v, 4-dim (DERIV-holographic literal) | ℂ_u^⊥ ∩ 𝕆, 6-dim (DERIV-holographic's sentence must change) |

## 2. The audit (both parties wrote theirs before comparing; the counts agreed exactly)

| Count | P2 | P2′ |
|---|---|---|
| New roots | 1 continuous — the ℂP² point (field on `Universe`, later crystallisation stage, or observer choice) | **1 discrete** — the choice of half in the ℤ/3-torsor {𝕆, ρ𝕆, ρ²𝕆} (the crystal does not pick one: V is ρ-invariant); or "a coordinate definition doing physical work" if the substrate is taken as the coordinatised `CDAlg ℝ 4` |
| Unproved links | 1 — 𝕆'_v is an octonion ⊃ ℍ_s (provable, moderate) — plus **1 conjecture on the root's moduli** (that the family is all of them, hence ℂP²) | 0 (that AXIOM-2's 𝕆 is a half is the reading itself, not a link) |
| Provable-now links | 1 (the codimension count) | 3: ℍ_s ∩ 𝕆 = ℂ_u; ℍ_s = CD(ℂ_u) by ℓ (five-to-eight-line Lean lemmas, sketched in the confirmer review); ρ exists, fixes ℓ, moves the half (numerically asserted; Lean owed) |
| Constitutional edits forced | none beyond flag 3 | **more than one sentence** (confirmer #4): DERIV-holographic's "4D gap"; T2 ("largest associative subalgebra of 𝕆 is ℍ") stops describing the observer's algebra at all, since ℍ_s is not inside the encoding; DERIV-3plus1's route (ℍ ⊂ 𝕆 maximal-associative ⇒ 1 + 3) is severed, two spatial directions leaving the encoding; the flag-3 draft on master (§5 INTERP entry, three-name vocabulary) is written under P2 and must be redrafted |
| Fit | DERIV-holographic literal; DERIV-sedenion (as on the ledger, "inter-cell boundary structure") natural — each universe has its own 𝕆'_v | exact to **PR #648's unmerged rewrite** ("𝕊 = 𝕆 ⊕ 𝕆ℓ, two copies of the encoding"), strained against the ledger's own "inter-cell" (under P2′ the half is global, the same for every universe, so "its own cell" in (O) has no per-observer referent — confirmer #3); DERIV-observation's ℂ is *selected* (see §4) |

## 3. The pressure-test (round 8) — what survived and what was withdrawn

| Round-7 claim (Gemini) | Test | Outcome |
|---|---|---|
| "P2′ *derives* DERIV-observation" | needs an unnamed premise **(O): what an observer can access is what is encoded in its own cell** — a physical reading of AXIOM-2's "encoding", not on record | **corrected twice**. Round 8: (O) is a root both readings need. Confirmer: (O) was written without its quantifier and the discriminator lived in that quantifier. With **O⊆** (accessible ⊆ encoded-in-cell), P2 + O⊆ says accessible ⊆ ℍ_s — no contradiction; EM's ℂ is picked by EM as before. With **O=** (everything encoded is accessed), P2 + O= contradicts — but P2′ + O= over-entails ("EM accesses all of ℂ_u") and still owes DERIV-observation's second step ℂ → ℝ. "Entails" also needs **(E)**: EM's ℂ = ℂ_u — not on record (DERIV-observation's ℂ is *electromagnetic*; ℍ has an S² of ℂ-subalgebras). **Honest statement: P2′ + O⊆ selects the observed ℂ (ℂ_u, up to the ℤ/3); P2 + O⊆ selects none; neither contradicts.** A *selection* discriminator, not a consistency one |
| "P2′ distinguishes chirality k = ℓU vs Uℓ" | ℓU = −Uℓ, same line, both in the high half; the sign is fixed by the doubling formula, not by the reading | **withdrawn**. P2′ fixes an asymmetry of *type* (U in-cell; ℓ, ℓU out) — an orientation of the pair (in, out) — not the sign of k. Chirality stays unforced under both |
| "predicts extreme anisotropy in spatial propagation" | no mechanism on record propagates anything along Im ℍ_s; the hosted physics is topological (Agda S³ H-space, charges) | **withdrawn** as a category error (Gemini's words) |
| The H-space product "crosses the boundary" (U·ℓ ∈ 𝕆ℓ) | ℍ_s is closed; products move between the halves of ℍ_s = ℂ_u ⊕ ℂ_u ℓ; the cell-component map x ↦ cdLo x is linear, **not** multiplicative ((ℓU)·ℓ = U — and (Uℓ)·ℓ = −U, sign corrected per confirmer #6 — has cell component ±U while the components' product is 0·0 = 0) | **kept as a fact**; Gemini's gloss that this "is exactly what quantum measurement means / hidden variables / decoherence" is **interpretation with nothing on record behind it** and is not adopted here. What is on record: under P2′ + O the map from the observer's algebra to what its half retains is linear and not a homomorphism. ("Lossy projection" is DERIV-arrow's phrase, not DERIV-observation's — struck here per confirmer #9.) |

## 4. Audit with (O) counted — corrected

| Count | P2 + O⊆ | P2′ + O⊆ |
|---|---|---|
| New roots | 2 (ℂP² point; O) | 2 (the ℤ/3 half; O) |
| Relation to DERIV-observation | consistent; selects no ℂ (EM's ℂ picked as before) | consistent; **selects** a ℂ — ℂ_u, one of three — and needs (E) "EM's ℂ = ℂ_u" to be *the* ℂ of DERIV-observation |
| Everything else | as §2 | as §2 |

The earlier "P2 + O contradicts DERIV-observation" is withdrawn. Note also that DERIV-observation is `derived_from` DERIV-holographic, the P2-literal entry: a premise under which P2 contradicted its own downstream entry would have been evidence against the premise before it was evidence against P2.

## 5. Verdict, kill, and what the audit cannot do

**Verdict (structural, confirmed):** P2′ asserts less than P2 — one discrete root against a four-parameter one; no octonion-existence link; no completeness conjecture — and, with the observation premise stated as an upper bound, P2′ selects the observed ℂ while P2 selects none. Cost: not one sentence but the set in §2 (T2's relevance to the observer, DERIV-3plus1's route, the flag-3 draft on master). "Canonical" is withdrawn: the crystal does not pick the half.

**Kill for P2′ + O (nominal — no observable on record can fire it):** evidence that an observer accesses non-commuting information (more than one ℂ of ℍ_s) at the encoding.

**What it cannot do:** say which reading is *true*. Both are consistent with every theorem; no experiment distinguishes them; the only discriminator is a selection made by a premise introduced in-round; DERIV-3plus1's "3 spatial directions = Im ℍ_s" under P2′ puts two of the three outside the cell — a *bookkeeping* fact today (the hosted physics is direction-blind topology), a physical claim only if a direction-dependent mechanism is ever written.

## 6. Next steps, with reasons

1. **Confirmer pass** — done (CONFIRMED-ON-CONDITIONS; all six applied here). It found the two load-bearing errors the dyad missed: the half is not canonical (ρ), and the discriminator was a quantifier artefact.
2. **Three Lean lemmas** in `CrystalHosting.lean`: `quatSpan_inter_lowHalf` (ℍ_s ∩ 𝕆 = span{1, U}), `quatSpan_eq_cd_double` (ℍ_s = ℂ_u ⊕ ℂ_u ℓ), and the ℓ-fixing order-3 automorphism ρ with ρ(𝕆_low) ≠ 𝕆_low (sketches in the confirmer review) — *because* they are the whole mathematical content of P2′ and the third is what turns "0" into "1". **Docstring correction owed on master:** `Hosting.lean` §11 and the boundary note say the order-3 elements of S₃ "send ℓ to another root" — they *fix* ℓ (verified), so `aut_hosting_equivariant` already covers them and the open S₃ item is narrower than stated.
3. **Beekeeper ruling** on P2′, **together with** the flag-3 split and the three-name boundary vocabulary (encoding octonion / holographic gap / seam), as qbp-architecture asked on #643 — *because* the flag-3 draft on master is written under P2 and must be redrafted if P2′ is ruled; and with the ℤ/3 named (which half is "the" cell is a datum, discrete).
4. **Premise (O), with its quantifier (O⊆), and (E)** go into the flag-3 draft as the observation clause of POST-observer-associativity, or as their own POST — *because* they are what link the boundary to measurement and were previously implicit.
