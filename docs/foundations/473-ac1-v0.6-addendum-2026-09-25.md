# #473 AC1/AC2 — v0.6 addendum (2026-09-25): the kill audited, attacked five ways, and reopened by ruling

**Status:** beekeeper-directed (2026-09-24: "I'm not precious about my ruling if it is proven wrong … let's work through 1–5 … in parallel"; 2026-09-25 ruling below). Author: qbp-oppenheimer (driver; the attacks ran as five isolated agents in their own worktrees). This addendum records (i) the audit of what `KILLED-locale-forcing-route` actually rested on, (ii) the five attacks with their sealed positions and outcomes, (iii) the corrections owed to the v0.4 proposition ladder (`473-ac1-first-link-2026-09-04.md`) and to the ledger, (iv) the ruling, and (v) the funded follow-ups. Nothing physical is ruled; the ruling is a process decision about the *status* of a route.

## 0. The ruling (beekeeper, 2026-09-25)

> "My general takeaway from this is that the kill statement has been falsified on two grounds. It was never proven and two is clearly more work to be done here. You're approved to resolve one through six. Specifically on six that is funded."

**Recorded meaning, precise:** the *status* "killed" (ratified 2026-09-05, "go with your recommendation") was not proof-backed — it rested on an argument (Prop 12) with numerical and prose legs — and two of its justifications are refuted (§3, §4). The kill's **conclusion** — no topological or measure-theoretic mechanism supplies the dynamical rule — was **not reversed** by any attack, and three of its legs are now theorem-grade. The route is therefore **reopened as OPEN research** (`FLAG-locale-forcing-route-reopened`), with **Prop 13(b)** as its kill and the honest class named (§6). The old record stays on the ledger, annotated, as history.

## 1. Audit: what the kill rested on (2026-09-24)

| Proposition | Claim | Evidence class at ratification | In Lean |
|---|---|---|---|
| 1 | spatial first link (locale from condensed set, ℝ included) | theorem | yes |
| 7′ | N supplies the measure; S₃ cuts invariant quadratic forms to Q_μ | theorem for forms + assumption (initial ensemble) | partly |
| horn-1 crux | initial ensemble = N's surface measure | ruling (PERMITTED postulate, MaxEnt) | — |
| 8 | an algebra-native locale is circular | argument | no |
| 9 | quench and anneal give different numbers | numerical (two scripts) | no |
| 10′ | a non-native tower needs a chart; the orbifold cover pushes no measure | argument, "not a theorem" | no |
| 15 | the only algebra-native cover of the vacuum S² pushes no measure | elementary/numerical | no |
| 16 | the algebra generates symmetries, never dynamics | (i) theorem; (ii) half theorem, "only ±ℓ" numerical; (iii) numerical | partly |
| 12 | AC2 not met by this route | "ARGUMENT + CRUX, not a theorem" (the document's own label) | no |
| 13(b) | reversal: a mechanism supplying the rule from topological/measure data | testable, live | — |

The process was rigorous (fifteen rounds under the MO, two REQUEST-CHANGES reviews, two heterogeneous confirmers, illusory consensuses caught). The central negative was never theorem-shaped as written: "no mechanism of this kind" needs a precise class of mechanisms.

## 2. The five attacks (sealed positions on #473, issuecomment-5808096164, before any run)

| # | Attack | Leg | Driver's sealed position | Outcome | Record |
|---|---|---|---|---|---|
| 1 | Gibbs β→∞ recomputed exactly; quench re-measured with three independent integrators | Prop 9 | anneal = 1/3 exactly (Laplace factor cancels the surface element); quench 0.146 ± 0.003 | **held on the anneal** (1/3 exactly, approach 1/3 − 0.554 β^{−1/2}; MCMC on the full potential from Haar and near-pole inits agrees at β = 10…1000); **missed on the quench**: exact **0.1416** (0.141587); every 0.146 on record carries a +0.0031 renormalised-Euler (h = 0.02) bias, 130σ paired. **New:** per-seed closed form b₀²(∞) = b₀²/(b₀² + √((1−b₀²)² − V₀)) — the four Gram invariants move on a straight ray under the descent | `research/473-kill-attack-1` |
| 2 | words in (s, ℓ, z), z over the 84 canonical zero divisors and 50 ridge points; iterated linear and nonlinear word-maps | Prop 16 (two-generator scope) | none reaches a non-pole vacuum | **held**: 5·10⁸ evaluations, 0 hits; σ = V/(1−b₀²)² never < 7·10⁻⁴. Sharpened: span(words) = all 16 dims, yet the vacuum is unreachable except ±ℓ; every ZD's L_z, R_z, ad_z share the spectrum {2×4, √2×8, 0×4}, top space and kernel are ridge planes, T(e₁+e₁₀) = K(e₁−e₁₀) | `research/473-kill-attack-2` |
| 3 | frame endomorphisms and valuations natural in V (Prop 12's admitted domain-theory loophole) | Prop 12 | every V-natural endomap factors through V*; nothing moves inside a level set | **FALSE as stated**: `vacNucleus` is V-natural and its fixed frame is Ω({V = 0}) with full topology (41 Lean theorems, 0 sorry). **Kill not reversed**: the nucleus is an idempotent projection with no clock, identical for both rules; `iInf_sublevel_eq_interior` (lfp of the sublevel filtration = interior{V=0} = ⊥); `natural_valuation_vanishes_of_const` (no V-natural reference measure). Prop 13(b) needs a metric-carrying locale — data beyond the frame | `research/473-kill-attack-3`, `proofs/QBP/Foundations/LocaleDynamics.lean` |
| 4 | the CD index group's Pontryagin dual (Cantor group) as an algebra-native profinite object | Prop 10′ | it pushes only uniform measure over index-labelled objects; nothing on the continuum | **held**; and Prop 10′ has a **wording gap** (it speaks of covers; the algebra also supplies a canonical profinite *group*): the object exists (characters = the sign automorphisms; within Ĝ, Fix(ker(Ĝ→Ĝ_k)) = CD_k, the tower's Galois correspondence) but is **inert** — Aut(𝕊) is a closed subgroup of GL(16, ℝ) preserving N, hence a compact Lie group, so any continuous action by automorphisms of a profinite group has finite image (no small subgroups; the unformalised identification Aut(𝕊) = G₂ × S₃ is not used). The finite counting measure puts the 15 imaginary basis vectors on the three cone points of S²(2,2,3) with weights (7,7,1)/15, ⟨b₀²⟩ = 1/15 = the β = 0 value | `research/473-kill-attack-4` |
| 5 | condensed Ext¹ as formation rate (CONJ-condensed-math-for-transition-state (a)–(c)) | Prop 13(b) directly | none (object undefined) | **rule-independent by construction**, proved: at any in-flight state the tangent space is 14-dim and the V-neutral directions form a 13-dim space (`finrank_vNeutral`, 15 Lean decls, 0 sorry) — every level-set invariant is blind to the rule. (a) ill-typed as a limit, impossible as a projection (𝕆 is simple); (b) ill-posed, reduces to a constant of the category, zero under repair; (c) no clock. Positive: the one non-Hausdorff object in reach is the orbit space of the *rule* — downstream | `research/473-kill-attack-5`, `proofs/QBP/Foundations/TransitionState.lean` |

## 3. Corrections to the v0.4 ladder (this addendum supersedes the cited sentences)

- **Prop 9.** Quench "0.146" → **0.1416** (exact 0.141587); the on-record 0.146 / 0.1462 / 0.1454 / 0.1436 carried a +0.0031 first-order Euler bias at h = 0.02 (`flowlib.step`, `flow_big.py`). Anneal "≈ 1/3" → **= 1/3 exactly**, approach 1/3 − 0.554/√β. The conclusion (two rules, two numbers; gap 0.19) is unchanged and now proved-grade. The same numbers recur in Props 12, 14, 16 and the one-line result; read them with this correction.
- **Prop 10′.** Last sentence re-worded: *the algebra supplies a canonical profinite object — the dual ∏ℤ/2 of its index tower, acting by sign automorphisms and giving the tower its Galois correspondence — and every profinite group acting by continuous automorphisms on a finite-dimensional algebra acts through a finite quotient (Aut is a compact Lie group; no small subgroups), so no profinite structure reaches S¹⁴.* Prop 15 gains: the branch locus carries the algebra's one chart-free discrete measure, the Cayley–Dickson grading-piece weights (7,7,1)/15 (not the G₂-isotypic 14 ⊕ 1), ⟨b₀²⟩ = 1/15 = E[b₀²] under the uniform measure on S¹⁴. Prop 13(a) stays unmet.
- **Prop 12.** The parenthesis "(Not 'a locale cannot supply dynamics by kind' … the objection is the rule-dependence)" and the driver's reasoning "a V-natural mechanism moves nothing inside a level set" are **replaced** by attack 3's theorems: the canonical frame-native descent has least fixed point ⊥, and no V-natural valuation exists; the vacuum sublocale IS V-natively available (the support), a measure on it is not. §6's "do not re-fund" line: categorical wrappers stay unfunded; **metric-carrying / enriched locales move to fund-only-if** — the honest class for Prop 13(b).
- **Prop 16.** Sharpened, not changed: with the canonical zero divisors as a third generator the words span all of 𝕊 and still reach no vacuum but ±ℓ; the "top plane on the ridge" observation is exact (funded Lean target (ii)).
- **Vocabulary firewall (new row).** "𝕆 → ℍ crystallisation" on the ledger means **subalgebra selection** — a point of G₂/SO(4), the hosting bundle over the vacuum manifold (`PROOF-crystal-hosts-quaternion`, `INSIGHT-octonion-quaternion-branching`) — never an algebra homomorphism: 𝕆 is simple, so no nonzero map 𝕆 ↠ ℍ exists. Sweep result: no ledger record asserts a morphism; the two documents that use "projection" language (`refounding-rederivation-verdict.md` "ℍ→𝕆→ℍ′ projection"; `substrate-foundational-concerns-resolution-2026-06-13.md` §2a) already mark it absent/refuted. No prose edit needed beyond this row.

## 4. Ledger changes (encoded by `scripts/encode_473_kill_reopen.py`, ledger 6.6.0)

| Record | Change |
|---|---|
| `KILLED-locale-forcing-route` | status killed → **marginal**; description appended with the 2026-09-25 ruling; `killed_note` records why the "killed" status was withdrawn (not proof-backed; two justifications refuted; conclusion not reversed). Historical record, not deleted. |
| `FLAG-locale-forcing-route-reopened` (new) | tracker, route OPEN; testable_when = Prop 13(b) with the honest class (a metric-carrying locale supplying both a per-level-set reference measure and a transport); notes list the theorem-grade legs and the funded follow-ups. |
| `CONJ-condensed-math-for-transition-state` | `testable_when` added (attack 5 §7.1: four prerequisites); `notes` record the adjudication — (a) and (c) withdrawn, (b) re-scoped to a descriptor, (d) untouched; status stays marginal. |
| `FLAG-rule-flow-open` | notes: quench 0.146 → 0.1416 (Euler bias); anneal = 1/3 exactly. |
| `REF-condensed-categorical-foundations-mathlib` | description clause corrected: Cond(Ab) is definable in Mathlib, its Ext groups are not evaluable (no EnoughProjectives, no computed condensed Ext, no LTE in Mathlib). |
| `REF-pyknotic-condensed-topos-status` | `last_tested_at` 2026-09-24 (verified against the pinned Mathlib: Condensed/Basic.lean). |

## 5. Funded follow-ups (item 6, beekeeper)

- **(i)** the quench is integrable in the Gram invariants: ODEs for (A, C, P, b₀) along an integral curve, the conserved quantity giving straight-ray motion, and the endpoint formula conditional on V → 0 (no existence claim) — `research/473-followup-6a`.
- **(ii)** the zero-divisor spectrum {2×4, √2×8, 0×4} and the ridge-plane identities T(z) = K(z′) on the sign table — `research/473-followup-6b`; closes the "observed, not derived" caveat in `NoAutonomousDynamics.lean`.

## 6. What would reverse the reopened route (its kill), stated honestly

A mechanism that supplies **both** a reference measure on each level set of V **and** a transport between level sets from topological or measure-theoretic data alone. Attack 3 proves the frame supplies neither; attack 5 proves level-set invariants are rule-blind; attack 4 proves profinite structure is inert. So the class is **metric-carrying (enriched) locales**, and any such candidate must show that the metric it carries is not simply N re-labelled (Prop 8's relocation test). Until then the route is open, not validated.

## 7. Provenance

Attack reports: `analysis/473-kill-attack/attack{1..5}_*.md` on the five branches (each with sealed expectations committed before its runs and a resource log). Lean: `LocaleDynamics.lean` (41), `TransitionState.lean` (15), both 0-sorry, axiom closure {propext, Classical.choice, Quot.sound}, foundations and layer gates PASS. Consolidated sort: #473 issuecomment-5810702167. Reviews: each branch enters the PR cycle (attacks 3, 5 and this addendum + ledger: Tier 3; attacks 1, 2, 4: Tier 2).
