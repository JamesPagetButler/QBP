# #473 AC1 — v0.5 addendum: rounds 13–15 (2026-09-05)

**Status:** addendum to `473-ac1-first-link-2026-09-04.md` (v0.4, PR #631, reviewed and unchanged). Rounds 13–15 were run at the beekeeper's direction *before* the #631 merge decision, with two inputs the earlier rounds lacked: the Cubical Agda layer (#575/#579/#595/#607) and the CTH ledger (extract in `analysis/473-dirac-probe/cth-extract-for-gemini.json`). Verbatim transcript: `473-ac1-rounds-13-15-transcript-2026-09-05.md`. Conversation MO applies; **the close is not self-declared** — it is handed to a Red Team confirmer (see §6). Research thread; no `Substrate/` file.

## 1. What died this time (the §7 record)

| Round | Claim | Who | Fate |
|---|---|---|---|
| 13 | "The algebra's operations on any state generate only H-space substructures; Prop 16 is Adams' theorem in algebraic dress" | proposed by the Red Team as T-B, accepted by Gemini in one turn and elevated | **FALSE.** Two generic imaginary units generate the *whole* 16-dim algebra by word length 5 (dim 10 at length 4; associator 0.4–0.5); the pair (s, ℓ) generates dim 4 with associator 0. The quaternion closure is a property of the Aut-forced element ℓ ([x, x, ℓ] = 0, Lean), not of the operations. `generic_maps_check.py` |
| 13 | "Autonomous dynamics require associative-ish composition" | Gemini | retracted (round 14) |
| 13 | "Lean tower-termination and the Agda H-space are isomorphic statements" | Gemini | corrected: both are *existence* witnesses; the uniqueness/non-existence direction (Hurwitz 1898; Adams 1960) is cited, not proved, and Adams is absent from the ledger |
| 13 | Everything accepted in one turn | Gemini | named as the fast-agreement tell, round 14 |

## 2. What survived, sharpened

**Prop 16, three layers** (the first two in Lean, `NoAutonomousDynamics.lean`; the third numerical, `generic_maps_check.py`, five independent t):

| Generators | Generated structure | Algebraic maps | Reachable vacua |
|---|---|---|---|
| one imaginary s | span{1, s} | s ↦ ±s | none new |
| s and the forced ℓ | quaternion subalgebra ℍ_s (S³, an H-space) | finite-order symmetries; (s+ℓ)/‖·‖ runs to the pole | ±ℓ only |
| s and an unforced generic t | **all of 𝕊** (dim 16 by word length 5) | x·t, t·x, [x, t] drive every start to the **zero-divisor ridge V = 1** (the landscape maximum), for every t tested; (x+t)/‖·‖ attracts to t; x·(x·t) leaves Im𝕊 | none: motion is *toward the seams*, away from the vacuum |

Reading: the algebra natively generates motion toward its zero divisors; motion toward the vacuum sphere (away from its poles) needs calculus (a gradient, hence a metric) or data beyond the algebra. Kill condition: an (x, t)-word whose iterates converge to a vacuum with |b₀| < 1.

**T-A, stated with directions.** Existence: composition algebras in dims 1, 2, 4, 8 (Lean, PROOF-normed-division-tower-existence); H-space on S³ (Agda, S3FromCD). Non-existence: no composition algebra at 16 (Hurwitz, cited); no H-space on S¹⁵ (Adams, **not in the ledger**). Prop 7′'s "what breaks at 16" is the fact both non-existence results witness. Proposed anchor: REF-adams-hopf-invariant-one.

**T-C killed:** the vacuum S² is not a Hopf base of the reachable S³ (that S³ meets the vacuum manifold only at ±ℓ). **T-D:** B(f ⋆ g) = B(f) + B(g) (PROOF-substrate-baryon-additive) is a theorem of ℍ, inherited by the tower, not a constraint at 𝕊; it does not meet AC2″.

## 3. Is AC2 the right question? Proposal AC2″ (for the beekeeper; not adopted here)

AC2 as written asks for *the single* prediction and conflates the route with the stack. The MDL gate behind it is right; the shape is wrong. Proposed:

> **AC2″.** The substrate earns in iff it supplies a distribution over crystals forced by the algebra given **three** named PERMITTED inputs — (i) the admissible class of initial measures, (ii) the dynamical rule, (iii) the b₀ → observable map — each recorded with a kill condition; the distribution carries a kill test on a measured quantity; and the prediction differs from the Haar baseline ⟨b₀²⟩ = 1/15 using only those inputs. Three is the honest count; it is MDL-positive only if the observable map is fixed by something other than the quantity under test.

Consistency note: the ledger's DERIV-crystallisation-asymptotic already models constants as asymptotic, never frozen — a distributional stance that AC2's "single prediction" sits awkwardly against.

## 4. CTH ledger: proposed artefacts (adopt or edit; nothing edited by the thread)

- **KILL-locale-forcing-route** | tier 3 | `killed` | internal-compute — the condensed/locale route cannot force a vacuum measure or a rule (Props 8, 10′/15, 12/16; reversal Prop 13); the spatial first link itself is *proved* (PROOF-spatial-first-link-condensed-locale) — this kills the forcing extension, not the mathematics. CONJ-condensed-math-for-transition-state and INSIGHT-condensed-math-deferred **stay marginal** (their machinery is untouched); INSIGHT-locale-condensed-chain stays coherent for its spatial claim with a pointer to the kill.
- **REF-adams-hopf-invariant-one** | tier 2 | coherent | theory-external — Adams, Ann. Math. 72 (1960): Sⁿ⁻¹ is an H-space only for n = 1, 2, 4, 8.
- **CHAIN-substrate-stack-to-b0**: AXIOM-2 → DEFN-cayley-dickson-doubling → PROOF-normed-division-tower-existence → PROOF-delta-landscape-descent → PROOF-alternator-vanishes-iff-commute → (Prop 15 vacuum S², #634) → (rule postulate, #635) → target PRED-b0-endpoint-distribution (#636/#637); status untested; weakest link = the rule postulate (the break inside the mathematics; the b₀ → observable map is the target's external dependency, #637).
- The route is a **leaf**: no existing chain uses any route anchor as a step.

## 5. Constitutional-layer flags (beekeeper only)

1. **DERIV-sedenion vs AXIOM-1.** "Seams where information CAN be destroyed" against "no physical process destroys information". The only entry touching it is FLAG-seam-dynamics-open (incoherent). Unreconciled.
2. **INSIGHT-fano-cube-universal-compute-cell** says crystallisation "selects 1 of 7 buses" — a pick, against DERIV-crystallisation-asymptotic and the distributional reading.
3. **DERIV-holographic** ("observers require associativity; largest associative subalgebra is ℍ") is supported by Prop 16 only through ℓ — the quaternion closure is ℓ's property, not a general "dynamics need associativity" fact (that claim was retracted).

## 6. §3 status — assessed by both parties, not declared

| Condition | Red Team | Gemini | Note |
|---|---|---|---|
| (1) Next steps reasoned | met | met | AC2″, three ledger artefacts, three flags, the ZD-ridge follow-up |
| (2) Assumptions surfaced + BOTE | partial → **met after `generic_maps_check.py`** (five t) | partial (asked for more than one t — done) | |
| (3) Grounded | partial → script now committed; Prop 16 in Lean | partial | the generic-pair facts remain numerical |
| (4) Shared understanding | met | met | two named retractions by Gemini; the Red Team retracted its own T-B first |
| (5) Easy answer pressure-tested | NOT MET at round 13; met after round 14 | same | |

Close: **converged-on-design, pending heterogeneous confirmation** (Red Team round 4 on this addendum). Beekeeper decisions carried forward: the three-horn crux (name Q_μ and the thermal family); Prop 12; the AC1 re-scope sentence; AC2″ adoption or not; the three ledger artefacts; the three constitutional flags; the #473 gate box; the #631 merge.
