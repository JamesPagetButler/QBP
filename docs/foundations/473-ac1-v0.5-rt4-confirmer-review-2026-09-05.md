# Red Team round 4 (confirmer) — #473 AC1 v0.5 addendum, rounds 13–15 (branch `research/634-prop16-no-autonomous-dynamics` @ c628f34)

Read in the directed order; Lean built (`run-bounded 8G 900 lake build QBP.Foundations.NoAutonomousDynamics`, exit 0, 2953 jobs); every script under `run-bounded`; own checks in `scratchpad/rt4/` (`gen_check2.py` exact generation, `mech_check.py` mechanism, `conv_check.py` convergence). Ledger read from the full `confluent-trust-inventory-v5_3.v0.3.json` (277 anchors, 22 chains), not the 48-anchor extract.

## §0 Findings

| # | Finding | Where | Sev | Who |
|---|---|---|---|---|
| 1 | **Layer-3 "dynamics" is power iteration.** x·t, t·x, [x,t] are *linear* in x (R_t, L_t, ad_t) and exactly skew-symmetric for imaginary t; normalised iteration is power iteration onto the top-σ eigenspace (multiplicity 4, eigenvalues ±iσ_max), on which R_t acts as σ·J, J² = −1. After 120 iterations every orbit is **period 2 up to sign** (residual ≤ 2e-14; at 60 iterations residual 5e-9–3e-7 ≈ (σ₅/σ₁)⁶⁰, straddling the script's 1e-8 threshold — hence its "not finite-order 1–100/100" scatter). The honest reading: a *transient* relaxation onto a 3-sphere of zero divisors, then a symmetry. "Motion toward the seams" is true; "not of finite order" is a threshold artefact. | addendum §2 row 3; transcript R15 table; `generic_maps_check.py` | 🟡 | Grothendieck |
| 2 | **Mechanism is structural, not numerical accident.** Σσ²(R_t) = 16·N(t) exactly (each e_k·t has norm N(t)), so σ_max > 1 ⇔ σ_min < 1: the dominant plane exists *because* the norm is non-multiplicative (Prop 7′); for ℓ, R_ℓ is orthogonal (Lean `N_ell_mul`), no dominant plane, no attractor. Top eigenspace on V = 1 for 100/100 t — observed, not derived; V = 1 points are exact zero divisors (σ_min(L_x) ≤ 2e-16, 200 samples). Open: an identity for N(xt) − N(x)N(t) placing the maximiser on the ZD locus. | §2 reading | 🟢/⚪ | Grothendieck |
| 3 | **Overclaim of Lean coverage.** §2 says the first two layers are "in Lean"; the Lean file's own caveat says only the invariant-subspace half of layer 2 is proved — "only ±ℓ reachable" rests on the numerical shape invariant. Row 2's "Reachable vacua: ±ℓ only" is not Lean. | §2 table header vs Lean docstring §Scope | 🟡 | Knuth |
| 4 | **Kill condition ill-posed.** "An (x,t)-word whose iterates converge to a vacuum with \|b₀\| < 1" — (x+t)/‖·‖ converges to t for *any* t; choose an unforced t on the 8-dim vacuum manifold and the kill fires trivially. Restrict to multiplicative words, or to generic t. | §2 | 🟡 | Sabine |
| 5 | **File title/docstring now contradict the addendum.** `NoAutonomousDynamics.lean` line 8: "symmetries, never dynamics"; addendum: "the algebra natively generates motion toward its zero divisors". Reconciled statement for both: *no autonomous dynamics toward the vacuum; the only non-symmetric behaviour is a linear transient onto the ZD locus.* | Lean docstring; §2 | 🟡 | Knuth |
| 6 | **CHAIN not schema-conformant.** Ledger chains are `{id, name, target_id, notes, source_ids, steps:int, status, weakest_link_id}`; membership is via anchors' `prediction_chain`. Three proposed steps are parentheticals with no anchor (Prop 15 S², rule postulate, b₀ map), the target `PRED-b0-endpoint-distribution` does not exist, and `weakest_link_id` must be an anchor id. Also omits input (i) — the unruled three-horn measure class, which moves ⟨b₀²⟩ 0.052→0.310 (μ) — as either a step or the weakest link. | §4 | 🟡 | all |
| 7 | **Flag 2 mislabelled constitutional.** `INSIGHT-fano-cube-universal-compute-cell` is tier 2, `untested`, INSIGHT — not DERIV/AXIOM. R14 packet defined constitutional as DERIV-*/AXIOM-*. Content of the flag is fair. | §5 (2) | ⚪ | Knuth |
| 8 | **"Only FLAG-seam-dynamics-open touches it" is false against the full ledger**: `INSIGHT-info-paradox-resolution` (t3 untested) and `PROOF-division-algebra-entropy-cone-mapping` (t1 incoherent) also touch information loss; neither reconciles DERIV-sedenion with AXIOM-1, so the flag stands, but the claim was checked against the extract only. | §5 (1); R14 §4 | ⚪ | Knuth |
| 9 | Killed-anchor naming: the ledger's one killed entry is `KILLED-f4-info-theoretic-justification` (a "Killed Hypotheses register"); proposal uses `KILL-`. Gemini's R14 draft had tier 1 / `proof-record` (not in vocabulary); RT silently fixed to tier 3 / `internal-compute`; Gemini "adopted verbatim" without noticing — the tell, mildly. | §4 | ⚪ | Knuth |
| 10 | Summary carries a claim Gemini never saw: "dim 16 by word length 5". Transcript has "dim 10 and growing". True (see below), but §1 attributes it to the round-13 death. Associator range is seed-dependent (script 0.40–0.53; R14 0.34–0.40; 50 pairs 0.21–0.84); addendum's "0.4–0.5" should say so. | §1 | ⚪ | Knuth |
| 11 | `generic_maps_check.py` needs ~4–5 min (killed at 240 s, finished under 300 s); no runtime note; period test threshold 1e-8 at 60 iterations sits on the convergence edge (#1). | script | ⚪ | Knuth |
| 12 | **AC2″ "differs from Haar 1/15" is toothless**: quench 0.146, anneal ≈1/3, ℓ-map 1 all differ from 1/15; every rule passes. The null must be the admitted class itself (see §3 below). AC2″ also drops "ℝ-non-trivial" and hard-codes one observable. | §3 | 🟡 | Sabine |
| 13 | Verified true: T-B false; ℓ is *unique* — {y : [x,x,y] = 0 ∀x} = span{1, ℓ} exactly (40 random x, dim 2, coords {0, 8}); exact rank-16 witness; Adams absent; T-C, T-D sound; Lean axiom-clean. | — | 🟢 | all |

## Reconciliation (transcript → addendum)

| Transcript item | R | Carried? |
|---|---|---|
| AC2′ proposed; accepted in one turn ("QM is distributions") | 13 | §1 row 4 / §3 ✓ |
| T-A "two witnesses of same truth" (glossed) → corrected to existence/non-existence table | 13/14 | §1 row 3, §2 T-A ✓ |
| T-B accepted + "Adams in algebraic dress" + "associative-ish" | 13 | §1 rows 1–2 ✓ |
| T-C kill confirmed; T-D theorem of ℍ, fails AC2′ | 13 | §2 ✓ |
| Q3(i) RT proposed marginal→incoherent; **Gemini rejected**, proposed KILL anchor | 13 | outcome ✓; RT's rejected proposal **not logged** (RT's own column) |
| Q3(ii) leaf; (iii) chain accepted, weakest link disagreement | 13 | ✓ |
| Q3(iv) three flags; Gemini: DERIV-sedenion "**must be corrected to scrambled/localized**" | 13 | flags ✓; proposed constitutional edit **not carried** |
| T-B computation incl. de Marrais pair dim 3 (verified: uv = 0, span dim 3) | 14 | table ✓; ZD row dropped ⚪ |
| T-B′; Adams-missing question; AC2″ (two inputs) + attacks (b)(c) | 14 | ✓ |
| Gemini retracts T-B and "associative-ish"; proposes order/attractor test | 14 | ✓ (test = R15 run) |
| Gemini: three inputs "on the edge of the MDL penalty box"; Haar 1/15 baseline | 14 | ✓ |
| Gemini KILL draft tier 1 `proof-record`; RT re-tiered | 14/15 | corrected text only (#9) |
| Weakest link = rule postulate (Gemini's argument accepted) | 14/15 | ✓ |
| R15 table: one t; "kill = a second t behaving differently"; t·x "mixed (some period 2)" | 15 | five t ✓; kill replaced (#4); period-2 nuance dropped (#1) |
| Gemini R15: "profound structural insight… completely vindicates" — accepted in one turn | 15 | **not logged as a tell** (see §3 (5)) |
| Gemini R15 (4) cites "x × x⁻¹ error" — a round-11 retraction | 15 | stale ref, harmless ⚪ |

Count: 16 items, 3 under-carried (RT's rejected status flip; Gemini's proposed DERIV edit; the round-15 one-turn acceptance), 1 over-carried (#10). Same shape as the last two live tests.

## Per-persona verdicts

**Grothendieck.** (a) Exact: integer imaginary s, t (`gen_check2.py`) give word-span ranks 4/6/10/16 at lengths ≤2/3/4/5 by rank mod p (a lower bound for the rational rank, hence a proof for that witness); 100/100 random pairs give 10/16. "Rank 16 at length 5" is Zariski-open, so one exact witness makes it true for *all* generic pairs. (b) See #1–#2: yes, every generic t ends at V = 1, and the reason is stated — power iteration of a skew operator whose non-conformality *is* Prop 7′; the ZD placement of the top eigenspace is observed, not proved. (c) T-B′ is correctly stated and stronger than written: ℓ is the unique element (mod ℝ1) with [x,x,ℓ] = 0 ∀x, so "ℓ's property" is exact. The Lean `assoc_self_ell` is precisely what `p_mul_ell_p` needs (`p·(ℓp) = N(p)·ℓ` from [p,p,ℓ] = 0 and pℓ = −ℓp), and `genByPair_ell_mem_quatSpan` is the closure induction claimed. (d) T-A table correct; `grep -i adams|hopf invariant` on the full ledger: empty; `PROOF-hurwitz` and `PROOF-hurwitz-quat` exist. (e) T-C kill sound (the identification was a category confusion: fibration base vs orbit space); T-D sound and understated: degree is additive under *any* H-space multiplication on S³ (Eckmann–Hilton), so it is not even ℍ-specific — it is ℝ-trivial under the original AC2 wording, which AC2″ dropped (#12). (f) Lean: 51 `#print axioms` lines, all ⊆ {propext, Classical.choice, Quot.sound} (46 full, 5 without choice); no `sorryAx`/`ofReduceBool`; the two `sorry`/`native_decide` hits are docstring text; both scope caveats are honest and the addendum violates one of them (#3).

**Sabine.** AC2″: "forced given the inputs" is vacuous — every computation is forced given its inputs; the entire content is whether inputs are fixed *independently* of the tested number, which the addendum asserts only for (iii) while (ii) already spans [0.146, 1] and (i) spans [0.05, 0.31]. Naming a knob PERMITTED with a kill condition does not make it cheaper in MDL bits; it launders it if the class is chosen after the data. The 1/15 null is a *no-dynamics* null, which any dynamics beats (#12). Baryon additivity cannot enter AC2″ — but only because AC2″'s form (a b₀ distribution) excludes it, not because a principled clause does; restore "ℝ-non-trivial". My one sentence: **The substrate earns in iff it fixes a measured number that ℝ + doubling leaves free, whose value does not move under any choice inside the PERMITTED classes it declares, those classes having been fixed before the number was looked at.** Layer 3 physically (#1): a spectral gap of a fixed linear operator, present in any non-composition algebra, carrying no information about crystallisation; the physical content stays the v0.4 negative (nothing algebraic selects the vacuum rule), now with a proof-shaped reason (Σσ² = 16N).

**Knuth.** Build reproducible with the symlinked cache; committed script reproduces the addendum's numbers (dim 10/16, V 1.000, x·(x·t) leaves Im 100/100, (x+t) fixed point) in ~4.5 min; #3, #5–#11 above.

## §3 verdict (rounds 13–15)

| Condition | Verdict | Why |
|---|---|---|
| (1) Next steps reasoned | MET (fix #4) | AC2″, three artefacts, three flags, ZD follow-up each carry a why; one kill condition ill-posed |
| (2) Assumptions surfaced + BOTE | PARTIAL | BOTE done (five t) but the assumption "these maps are a dynamics" was never examined; they are linear; the "not finite order" reading is a threshold artefact (#1) |
| (3) Grounded | PARTIAL | Lean built and clean; script committed; but §2 overclaims Lean coverage (#3) and the length-5 claim is grounded by script only (now exact here) |
| (4) Shared understanding | MET | two named Gemini retractions; RT retracted its own T-B first; one stale citation |
| (5) Easy answer pressure-tested | MET for T-A/T-B/AC2′; **NOT MET for the round-15 ZD-ridge reading** — "the algebra generates motion toward the seams" was accepted by both in one turn ("profound… completely vindicates"); the question "what kind of map is x ↦ xt?" was one line away, exactly the round-12 shape one level up |

**NOT CONFIRMED as written. CONFIRMED on the design** (three-layer Prop 16 as a structure; AC2″ as a *proposal*; KILL and REF anchors; flags 1 and 3; the leaf claim — verified via `prediction_chain`: the three route anchors chain only among themselves) **conditional on:** (i) layer 3 restated per #1–#2 and the Lean docstring/title reconciled (#5); (ii) §2 "in Lean" corrected (#3); (iii) kill condition restated (#4); (iv) CHAIN rebuilt on real anchor ids with input (i) present, or filed as "proposed, anchors pending" (#6); (v) flag 2 relabelled, flag 1's "only entry" claim corrected (#7–#8); (vi) the three under-carried items and the round-15 tell logged in §1/§6; (vii) AC2″ text restores "ℝ-non-trivial" and states the invariance-under-class criterion for all three inputs (#12).

## AC verification vs #473

| Item | Text | Status | Evidence |
|---|---|---|---|
| Gate 1 | Do not start until phase-1 tower built | MET | issue header: phase 1 complete |
| Gate 2 | Earns in only by forcing ONE ℝ-non-trivial prediction | NOT MET | none forced; addendum proposes changing the gate (AC2″) — beekeeper decision, not a pass |
| AC1 | First link formalised; toward a forcing argument for ℝ | PARTIAL (unchanged by rounds 13–15) | `SpatialFirstLink.lean` (PROOF anchor) ✓; forcing argument negative (Props 8/10′/12/15/16) pending the Prop 12 ruling and the re-scope sentence |
| AC2 | State the single falsifiable ℝ-non-trivial prediction; else document and downgrade | NOT MET; documented | route killed (KILL anchor proposed); T-D is ℝ-trivial; AC2″ proposed *instead of* claiming AC2 — the "downgrade" is a beekeeper action still owed |
| AC3 | If AC2 passes its kill-check, propose the layer-below | MOOT | AC2 not passed |

## Bottom line

**REQUEST-CHANGES** (on the text; the design is confirmed). The mathematics the dyad got right is real and worth keeping: T-B is false, two generic elements generate all of 𝕊 (now exact), ℓ is the unique left-alternative-nucleus element, Adams is missing from the ledger, T-C and T-D are dead. What the dyad got wrong is the one thing it agreed on fastest: layer 3 is not a new kind of algebraic dynamics but power iteration of a skew operator — a transient onto a zero-divisor 3-sphere followed by a period-2 symmetry — with the transient existing *because* the norm stops composing at 16. That is a better result than the one written, and it belongs in the addendum, the Lean docstring and the kill condition before this close counts. Beekeeper items unchanged: Prop 12; the three-horn crux; AC2″ (with #12); the ledger artefacts (with #6, #9); flags 1 and 3 (flag 2 is ordinary); #631 merge.

— Red Team round 4 (confirmer) — Sabine · Grothendieck · Knuth
