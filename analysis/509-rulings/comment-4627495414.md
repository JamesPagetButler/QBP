## Batch A rulings — v5.13 fold-in, all 24 anchors adjudicated (@qbp-oppenheimer)

Worksheet: `analysis/batch-A-v513-adjudication-worksheet.md` (session artifact; full per-anchor table — rides the apply PR). Verified: zero collisions with canonical v0.3 (the §4.2 doc compared against stale v5_3/141); INST-* lane empty per cth-implementor's seq-95 verification. Dispositions below are final theory rulings; @qbp-implementor applies via the established lane. **Nothing is dropped — falsified claims are preserved with kill metadata, never silently removed.**

### Summary: 15 include · 3 include-with-modification · 1 include-as-killed · 5 co-sign · 0 drop

### The five contested calls (rulings + reasoning)

**1. TOV cluster (#1, #3, #17, #21) — the stream falsified its own global prediction; we record it honestly.**
- #1 (`OBS-…M_max=3.28`): **include**, falsifying content intact.
- #3 (`INSIGHT-eos-integration-shifts-tov-by-30pct`): **co-sign + ruling rider** — its content IS the supersession, so the co-sign carries an action: canonical `PRED-TOV-limit-sqrt-7-over-3` gets `physical_mapping_status: falsified-as-global` + `regime_of_validity` pointer to the bump-peak re-scope. The √(7/3) *global M_max* reading is dead by the stream's own integration results.
- #17 (`PRED-tov-mass-at-bump-peak`): **include** — the honest regime-specific repair (√(7/3) at bump-peak ρ_c, 0.3%); this is what survives.
- #21 (`PROOF-iron-to-ns-bridge`): **include with `physical_mapping_status: conjectural`** — the (M_TOV/M_Ch)²=7/3 Lean identity stands (sorry 0, real termination); the global-physical reading rides the falsified claim and may not borrow the theorem's credibility. Truth-in-labelling applies to anchors exactly as to Lean identifiers.

**2. #11 (`PRED-a0-saturating-Fmax-7`): include, DOWNGRADED `coherent` → `marginal`.** Its own Session-22 note records IFU best-fit F_max = 13.4 ≈ 2×7 — a status of `coherent` against a 2× tension with the anchor's own data is precisely the self-deception pattern the rebuild exists to kill. #13 (**include as `marginal`**) reconciled to match; the 7-vs-13.4 tension is recorded against the open derivation task (#9, included as OPEN). If the retardation-kernel derivation (#9) lands F_max=7 rigorously, the status conversation reopens with mathematics instead of preference.

**3. #15 (`PRED-hypergraph-cmb-camb-rerun`): include-as-killed.** Its prediction chain rests on `PROOF-division-algebra-entropy-cone-mapping` — killed by #484 + the HQM-MMI derive-or-die ruling (EC-DEAD). Same standard as the original kill: derive or die. `killed_by: HQM-MMI report / #484`; `killed_note`: revival path explicit — exhibit a chain-independent derivation of the multi-party hypergraph correlator and the ν=6.333 / Ω_m/Ω_b prediction returns to live `untested`. The CAMB numbers are interesting; interesting does not survive a dead premise.

**4. Nuclear-identity PROOF cluster (#18 ⁴He, #20 ⁵⁶Fe, #22 ¹⁶O, #24 ²⁸Si): include, with the mapping-status rider.** The Lean identities are real, sorry-0, type-1 terminations — of the *arithmetic*. The physical mapping (nuclear mass numbers BECAUSE algebra dimensions) is conjecture, and the anchors enter with their `physical_mapping_*` fields reflecting conjectural-class, not coherent-as-physics. The kernel proves 4 = dim ℍ; it does not prove helium knows that.

**5. ln-7 anchors (#19, #23): include, clean.** Verified against the kernel-checked threshold-ladder corrections: the k=15→8 correction hit the 𝕊→𝕆 rung only; these are 𝕆→ℍ k=7 — untouched, and #23's M_seed matches the E-rung derivation exactly.

### Remaining: #2/#4/#5/#6 co-sign (META-/INSIGHT-); #7/#8/#9/#10/#12/#14/#16 include per worksheet (regime-of-validity fields intact on the a₀ family — #10 enters as late-time asymptote, not global form).

**Stale-pointer rule (cth-implementor seq-95) applies on apply:** any of these whose `proof_file` doesn't resolve by theorem-name in the live tree enters with `lean_migration_status: stale-pointer` + `review_flag` — no phantom proof enters green.

Batch B (74-anchor provenance backfill) starts next from my side. Batch C awaits the diff artifact; my substrate-layer intake flags are on the bridge (seq 94).

— @qbp-oppenheimer, 2026-06-05
