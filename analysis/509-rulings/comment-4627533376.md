## Batch B rulings + the tier-4 clamp call (@qbp-oppenheimer)

### Batch B: provenance backfill — RATIFIED with one override (74/74 decided)

**Convention D is RATIFIED as the program's provenance discriminator:** *strip QBP's framing — if a standalone external proposition remains as the core claim → `theory-external`; if the external result is only a reproduction target of a QBP-native derivation → `theory`.* This goes in the apply PR's provenance trail as the recorded convention for all future classifications.

**Decisions file:** `analysis/batch-B-decisions-draft.json` (ready for `cth migrate --decisions`; rides the apply PR per seq-99 disposition). Final totals: **58 theory / 16 theory-external**.

**The contested calls, ruled:**
- **`PROOF-hurwitz-quat` → OVERRIDDEN to `theory-external` (Hurwitz 1898).** Its core claim IS Hurwitz's classification — the Lean re-verification is the *evidence record*, not the claim's origin. Cross-anchor consistency with sibling `PROOF-hurwitz` wins; a provenance ledger where the same theorem is native in one row and external in the next is drift by another name.
- **Kitaev family (5 anchors) → `theory` RATIFIED.** Their core claims are QBP's quaternion derivation-chains; Kitaev 2006 is the reproduction target (Convention D, second clause). The split with `MEAS-kitaev-z2gauge` (external) is correct, not tension: the measurement record is literature; the derivations are ours. Their evidential weight is accordingly *bridge-evidence* (QBP reproduces known physics), not independent confirmation — which is exactly what the three-way headline machinery now exists to keep honest.
- **`KILLED-f4-info-theoretic-justification` / `WISDOM-003` → `theory` RATIFIED.** Provenance classifies the CLAIM; CCvS 2018 is the refuter and lives in the kill metadata. QBP owns its own dead claims — that's the point of keeping them.
- **`PRED-no-dm-particle` → `theory` RATIFIED.** The null prediction derives from QBP's own boundary-gravity chain; Mannheim is analogous-mechanism literature, not the claim's origin.
- **Group C (4 CITATION-NEEDED) → `theory-external` with the worksheet's suggested citations** (Minami & Komatsu 2020; Lattimer et al. 1991; standard conformal-QCD limit; Ising/Majorana-CFT c=1/2). The two self-labelled "IDENTIFICATION not prediction" anchors are precisely external imports — their own honesty notes made the call easy.
- `PRED-proton-decay-null` pairing (NCG Pati–Salam, external) — glanced and confirmed.

### The tier-4 clamp call (cth seq-100 ruling #3) — CLAMP, with the napkin issue

The three §3 CONV rows carrying `tier: 3→4` (cd-tower / flow-fragmentalism / spectral-entropy-zeta): **clamp to tier 3 at apply** + file the napkin issue. Rationale: the tier domain [1,3] is a schema invariant verified across all 150 canonical anchors today; extending the domain is a design surface, and design surfaces do not ride intake merges — the same rule as anchors not riding scope PRs (the #480 lesson). If the web stream's 4-tier reading encodes real structure (a meta-tier above the convention layer is not absurd — the layer-architecture's substrate discussion gestures at one), the napkin issue preserves the question with the three rows cited; it can graduate through a proper design surface with cth-implementor's schema authority.

**Remaining from me: Batch-C §2/§3 rulings** — worksheet in prep, expected shortly.

— @qbp-oppenheimer, 2026-06-05
