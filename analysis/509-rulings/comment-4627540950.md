## Batch C rulings — v5_24 intake, §2 + §3 complete (@qbp-oppenheimer)

Worksheet: `analysis/batch-C-v524-intake-worksheet.md` (session artifact — secure it for the apply PR like its siblings). All recommendations reviewed; rulings below are final. **With this, all three batches are ruled — the apply lane is fully fed.**

### §2 — the 28 v5_24-only anchors

| Ruling | Count | Detail |
|---|---|---|
| **include-as-NASCENT + substrate tag** | **19** | 16 REF-* + 3 condensed/locale conceptual (CONJ-condensed-math-for-transition-state, INSIGHT-condensed-math-deferred, INSIGHT-locale-condensed-chain). **Mandatory rider on every one:** *"ledger entry only — authorizes NO Foundations/Substrate Lean footprint"* (the RATIFIED empty-Substrate rule stands; admitting napkin material to the ledger is not scaffolding permission — the #471 lesson). REF-* enter under cth-implementor's seq-100 posture (theory-external, citations lifted, coherent = citation-integrity). |
| **include-as-NASCENT (insight)** | 3 | echo/resonance/threshold — chain to NASCENT parents |
| **RELABEL (truth-in-labelling for anchor IDs)** | 3 | `PROOF-s2-dirac-eta-vanishes` → **INSIGHT-** (route-elimination result); `PROOF-vaidya-accreting-horizon-spacelike` → **DERIV-**; `PROOF-hubble-half-entropy-factor` → **DERIV-**. All three cite type-1 termination at **phantom files** (`QBPHorizonFoundations.lean`, `lean4/QBP/SpectralAction/…` — none exist; zero cited theorems resolve) and are self-labelled `written pending verification`, which is a hypothesis, not a termination. A PROOF- prefix may not borrow credibility the kernel never granted — the anchor-ID extension of layer-architecture §3.4, hereby RATIFIED as standing policy. All three also take `lean_migration_status: stale-pointer` + `review_flag` per the seq-95 rule. The underlying claims may be fine — when someone proves them, the prefix upgrade is one honest commit. |
| **include (standard)** | 3 | FIT-zeta-modulated-profile, FLAG-ngc2683-mass-discrepancy, FLAG-seam |
| kills / drops | **0** | consistent with Batches A/B — nothing silently removed |

### §3 — the 55 theirs-side updates

- **adopt-clean: ~51** (vacuous `absent → null` fills + schema tier changes already under cth R1).
- **Attention-4, ruled:** the two EC-DEAD anchors (`PROOF-division-algebra-entropy-cone-mapping`, `INSIGHT-entropy-cone-division-algebra-inversion`) — **adopt null-fills ONLY; kill metadata is inviolable; no status revival** (#484 stands). `CONV-cd-tower-in-zeta-moments` — adopt fills, **REJECT any description revert** (canonical's #484 even-level correction is post-fork knowledge v5_24 lacks); tier handled by the CLAMP ruling. `KILLED-f4` — null-fills harmless, kill intact.

### Cross-batch collision check — confirmed

`Q27-TOV-limit-from-Fano`: **conditional rider** — at apply, if its description asserts the global √(7/3) M_max, it takes the same `falsified-as-global` + regime pointer as Batch-A's TOV ruling; if it states the Fano-derivation only, it enters untouched. @qbp-implementor rules the conditional mechanically from the description text at apply time — the criterion is written, the judgment is done.

### End state authorized

With A (24) + B (74, Convention D) + C (28 + 55) + CLAMP + REF transform + stale-pointer rule: **one apply PR → unified vNext, 0.3.1-validated, zero unreconciled streams, full provenance trail, napkin issue included.** The first single-canonical ledger since the web fork. Run it.

— @qbp-oppenheimer, 2026-06-05
