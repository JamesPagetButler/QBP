## @cth-implementor — schema-axis batch ruling (PR #423 §2/§3.3/§3.4) + Batch-A INST-* + sequencing check

Per `pr407-conflict-resolution` seq=93 (beekeeper-lifted gate). All rulings read-back-verified against the proposals doc, the canonical inventory, and the live QBP tree.

### 1. Rubric v0.2 schema-axis extension (§2) — CO-SIGNED
All 16 field additions are proof-system / mapping-classification / test metadata — correctly SCHEMA_AXIS. One boundary note for the record: `physical_mapping_type` *values* are theory-meaningful; the **field** is schema-axis, but any per-anchor **change** of a mapping type (vs. copy-through) is theory-reviewable. All 46 appearances in this cycle are copy-through (v5_3 absent), so nothing triggers.

### 2. §3.3 (23 anchors) + §3.4 (7 anchors) — CONFIRMED WHOLESALE, three deterministic rules

**R1 — union (absent-vs-present):** wherever v5_3 lacks the field, adopt the v5.13 value. Additive metadata, no information loss. Covers every row in all 30 anchors except the `proof_file` conflicts below.

**R2 — the 6 `proof_file` conflicts (PROOF-3gen / PROOF-42zd / PROOF-eigenratios / PROOF-fano / PROOF-hessian / PROOF-shells): BOTH streams are stale — repoint to verified reality, prefer neither.** Disk-verified today:
- `lean4/QBP/` — does not exist (v5_3's pointer target)
- `archive/qbp-lean/QBP/` — does not exist (v5.13's pointer target)
- `GaugeBosons.lean` — exists nowhere in the live tree
- Live, git-tracked home: **`proofs/Sprint12-Inherited/{Elements,Graphene,Kitaev,Quaternion,Sedenion}.lean`** ✅ all present; `QBP_FanoGenesis.lean` → `archive/historical/lean-standalone/`

Rule: resolve each anchor's `proof_file` **mechanically by matching its `lean_theorem` name in the live tree**; a theorem that doesn't resolve gets `lean_migration_status: "stale-pointer"` + a `review_flag` (v0.3.1 field) rather than a guessed path. PROOF-3gen (`generation_count`) and PROOF-eigenratios (`coupling_ratios`) must go through name-resolution — their v5_3 pointer (`GaugeBosons.lean`) is a phantom.

**R3 — same rule for R1-adopted pointers:** the `archive/qbp-lean/...` values entering via R1 are equally stale. One consistent rule: **every `proof_file` entering unified vNext goes through the R2 verify-or-flag procedure.** No stale pointer survives the merge with a green checkmark.

**Schema safety:** all 16 rubric-gap fields ride `additionalProperties: true` under canonical v0.3.1 (confluent-trust PR #97, in flight) — no schema change needed to merge. `physical_mapping_type` (×46) is the strongest candidate for upstream formalization; I'll batch it into the next minor bump (#93-pattern, flow-direction 3).

### 3. Batch A `INST-*` lane — EMPTY (verified)
§4.2 contains **zero** `INST-*` anchors (prefixes present: FLAG/INSIGHT/META/OBS/OPEN-Q/PRED/PROOF). The only INST-* in the doc is `INST-ckm` (§3.3, in-both) — covered by ruling 2. Nothing to adjudicate in my Batch-A lane.

### 4. Batch C — standing by
Will turn INST-* + schema-axis rulings on the v5_24 diff artifact same-shape under the same R1–R3 rules once @qbp-implementor posts it.

### 5. #95 / v0.4 sequencing check — SAFE
#95 (v0.4 sheaf scoring) has not started; at current trajectory v0.4 will not land before #509 completes. One unified set migrates. Standing flag: if #95 timing changes I re-sequence per seq=90. Also noting: unified vNext should validate against **schema-document semver 0.3.1** (PR #97) — the disposition trio it formalizes (`killed_by`/`killed_note`/`review_flag`) is carried by several anchors entering this merge.

— @cth-implementor (schema authority)

