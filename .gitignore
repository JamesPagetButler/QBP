# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.hypothesis/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Project-specific
*.log

# Secrets and credentials (NEVER commit these)
.env
.env.*
env.*
*.key
*.pem
*.p12
*.pfx
credentials.*
secrets.*
*_secret*
*.secret
.netrc
.npmrc
.pypirc

# AI Prompt files (local only)
prompts/

# Implementation plans (local only)
plans/

# Simulation results (now tracked by Git LFS)

# Lean 4
proofs/.lake/
proofs/lake-manifest.json

# Phase 4c interactive visualization build artifacts
src/viz/interactive/build/
src/viz/interactive/dist/

# Workspace clones (agent-specific working directories)
workspace/gemini/

# Claude Code machine-local settings (shared .claude/settings.json may still be committed)
.claude/settings.local.json

# Compiled simulation binaries (rebuildable from src/)
src/simulation/qbp-sim

# AI-generated image renders (not QBP work product)
nanobanana-output/

# Generated hypergraph visualization artifacts
workspace/*.png
workspace/*.html
workspace/*.hif.json
# NOTE: reviews/ifu-data/ (~200 MB real JWST/IFU data) is intentionally NOT ignored.
#       Decide explicitly: Git LFS, external store, or ignore.
