cubical-0.9 (agda/cubical, pinned by commit in CI)

# Library-dependent Cubical Agda (the Buchholtz–Rijke S³ port — COMPLETE)

Unlike `proofs/agda/` (builtins-only, self-contained), these files depend on the
**agda/cubical** library and are type-checked under `--safe` with it.

**The port is complete: `S³-HSpace : HSpace (S₊∙ 3)` (S3FromCD.agda), `--safe`,
0 holes, 0 postulates, end to end.** Port of Buchholtz–Rijke (arXiv:1610.01134)
"The Cayley-Dickson Construction in HoTT", faithful to their Lean-2 HoTT
formalization (vendored, Apache 2.0, in `analysis/drive-agda-sketch-vv/port/br-lean2-source/`).

## The chain (checked in this order by dependency)

| File | BR source | Content |
|---|---|---|
| `Diamond.agda` | join.hlean | diamonds in joins: `degSquare`/`hdegSquare` (J), `diamond`, `vdiamond`/`hdiamond`, `symmDiamond`, `twistDiamond`, `apDiamond` (a one-liner — `push` is a binary constructor) |
| `CDJoinBR.agda` | imaginaroid.hlean | `negS`/`starS` on `Susp A₀`, the `CDLaws` record, BR lemmas 1–4, `genDiamond` (suspension induction — the step that closes the coherence square that resisted 7 direct attempts), `cd-mul`, `CDJoin-HSpace : HSpace (join S S , inl 1)` |
| `CDLawsBool.agda` | quaternionic_hopf.hlean | `A₀ = Bool`, `neg₀ = not`: mult = S¹ multiplication conjugated along `S¹IsoSuspBool`; laws via `negS-id`, `star-to`, `rotInv-1/2`, `S1-AssocHSpace` ⇒ `HSpace (join SuspBool SuspBool)` |
| `QBPS3HSpace.agda` | — | the reduction: `HSpace≃` (pointed univalence transport) + `S³-HSpace-from-join : HSpace (join∙ S¹ S¹) → HSpace (S₊∙ 3)` (`IsoSphereJoin 1 1`) |
| `S3FromCD.agda` | — | the wiring: `Iso→joinIso` join congruence, `JoinS¹-HSpace : HSpace (join∙ (S₊∙ 1) (S₊∙ 1))`, **`S³-HSpace : HSpace (S₊∙ 3)`** |
| `SkyrmionCharge.agda` | — (#595 step 1) | the baryon-number primitive on the same `S₊∙ 3`: `baryonNumber = degree 3` with pinned contracts — B(vacuum)=0, B(hedgehog)=1, B(f ∙Π g)=B f+B g, B(-Π f)=−B f, `skyrmionℤ z` realizing every charge z, `π₃(S³)≅ℤ` generated by the hedgehog, and `∥S³→S³∥₂ ≅ ℤ` (complete classification) |
| `SubstrateCharge.agda` | — (#607) | the substrate-connection theorem: for the #575 quaternion μ, **B(f ⋆ g) = B f + B g** where `(f ⋆ g)(x) = μ (f x) (g x)` (`baryonNumber-quaternionProduct-correct`), plus translation invariance `B(μ a ∘ f) = B f` and `B(μ a ·) = +1` for EVERY unit quaternion a. Proved behind a module-abstraction barrier (`SubstrateLink (H : HSpace (S₊∙ 3))` — μ stays a neutral variable; Eckmann–Hilton interchange by suspension induction + J-only path algebra), instantiated at `S³-HSpace` by plain application. Checks in ~10 s / <700 MB — the transported μ is never normalized. No `AssocHSpace` anywhere (S⁷-compatible per #579) |
| `AssocTransport.agda` | — (#579 A) | `AssocHSpace-subst : (P : A ≡ B) → AssocHSpace H → AssocHSpace (subst HSpace P H)` (J on P, H generic — no μ ever projected) and `AssocHSpace≃` along `HSpace≃`: **`AssocHSpace S³-HSpace ⇐ AssocHSpace JoinSuspBool-HSpace`** |
| `HopfNoAssoc.agda` | — (#579 C) | verbatim MIT-attributed extraction of `Cubical.Homotopy.Hopf` lines 36–198 (the assoc-free half: `μ-eq`, `Hopf`, `TotalSpaceHopfPush`, `joinIso₁`, `IsoTotalSpaceJoin`) as `module HopfNA (e : HSpace A) (conA)` — the library header demands `e-ass : AssocHSpace e` that only the later join-of-joins `joinIso₂` uses |
| `QuaternionicHopf.agda` | — (#579 C) | **the quaternionic Hopf fibration** on the substrate μ: `HopfS³ : S₊ 4 → Type` with `HopfS³ north ≡ S₊ 3` (refl), gluing = right-multiplication `μ-eq a`, `HopfS³-fibre∀`, **`TotalHopfS³ ≃ S₊ 7`** (S³ * S³ ≃ S⁷); the reduction `S³-AssocHSpace-from-join : AssocHSpace JoinSuspBool-HSpace → AssocHSpace S³-HSpace` and `S³-AssocHSpace-from-cubes : (7 cubes) → Filler → AssocHSpace S³-HSpace`; `module WithAssoc (S³-assoc)` instantiates the full library `Hopf` module and proves `Hopf≡HopfS³`. The kernel `S³-AssocHSpace-kernel = AssocHSpace JoinSuspBool-HSpace` is stated as a type — **no inhabitant claimed** (#579 AC1 open) |
| `CDAssocReduction.agda` | — (#579 B) | generic over `CDLaws` **plus** a separate `CDCommLaws` record (⊗-comm, star-⊗, star-star, ⊗-negˡ — `CDLaws`/`CDJoin-HSpace` untouched, S⁷-safe): the 8 corner paths eq₁…eq₈, `assoc-corner` (8 clauses), `assocₓ`/`assoc-y`/`assoc-z` (12 push-squares reusing the same 8 paths) = **20 of the 27 clauses of `μ-assoc`**; the 6 two-push cubes + 3-push 4-cube stated as types with pinned boundaries; `AssumingCubes.μ-assoc` assembles them; `CDJoin-AssocHSpace : Filler → AssocHSpace CDJoin-HSpace` |
| `CDAssocBool.agda` | — (#579 B) | `CDCommLaws not SuspBool-CDLaws`: `·-comm` and `invLooper-·` on S¹ via `wedgeconFun 0 0` (no point-level `·-comm` in the pinned library); `JoinSuspBool-AssocHSpace-from-cubes` |
| `CDAssocCubesStatus.agda` | — (#579 B) | existence status of the open cubes: `isConnected 4 (join SuspBool SuspBool)`; each two-push cube is **merely** inhabited pointwise (`∥ Cube-··-·-at … ∥₁`, = π₂(S³)=0) and so is `Filler-at`; the 3-push 4-cube is NOT reachable this way (would need π₃(S³)=0, false) — it is the irreducible content |

### Performance discipline (#579 files)

Nothing in the #579 files computes with the transported `S³-HSpace` μ.
`AssocTransport` proves its lemma by J with the H-space a generic argument
(at `refl`, `substRefl`), so `AssocHSpace≃` is applied to `S³-HSpace` without
unfolding it. `HopfNoAssoc.HopfNA` is a module over a generic `HSpace A`,
instantiated by plain application in `QuaternionicHopf` (5–11 s / <530 MB).
`CDAssocReduction` works on the *un-transported* `cd-mul` over `Susp A₀`
where every clause is a constructor computation. The one thing that would
diverge — normalizing `pushMulSquare` (a `transport` of a diamond along four
base paths) to attack a two-push cube — was not attempted; see the
REFINEMENT-LOG iteration-16b entry for the naturality route instead.

### Performance discipline (SkyrmionCharge.agda)

Concrete-index instantiations of `Cubical.Homotopy.Group.PinSn` lemmas
(`πₙ'Sⁿ≅ℤ 2`, …) require `--lossy-unification` (as PinSn itself uses), and
their contract statements must be spelled **verbatim** in the library's form —
routing them through local aliases makes conversion normalize the full iso
(observed >30 min / 6 GB before kill). Degree-based statements (`degree 3`)
are insensitive to this. Projecting `μ`/`μₗ` out of the transported
`S³-HSpace` and *normalizing* them (e.g. computing "B(μ 1 ·) = 1" by brute
force) diverges (>8 GB) — that route stays forbidden. The theorem itself is
now proved in `SubstrateCharge.agda` via the module-abstraction barrier:
all reasoning happens over a *generic* `H : HSpace (S₊∙ 3)` whose μ is a
neutral variable, and `S³-HSpace` is substituted only by application, which
the checker never normalizes (~10 s / <700 MB, vs the >8 GB divergence).

## Axiom cleanliness (#578 AC4)

Agda's `--safe` is transitively enforced (a `--safe` module may only import
`--safe` modules), so the successful check certifies the entire dependency
closure free of `postulate`/`primTrustMe`/`REWRITE`/positivity- and
termination-escapes. Only the cubical primitives (`hcomp`/`transp`/`Glue`)
remain.

## Convention note (#578 AC5)

This port uses **Baez's** CD convention `(a,b)(c,d) = (ac − db*, a*d + cb)`
(faithful to BR). The Lean #474 foundation (`proofs/QBP/Foundations/CDAlg.lean`)
uses **Schafer's** `(ac − d̄b, da + bc̄)`. Both standard; canonically isomorphic
via `φ(a,b) = (a, b*)`. Do not identify components across the two without
routing through φ. Full derivation: `analysis/drive-agda-sketch-vv/port/REFINEMENT-LOG.md`
iteration 13.

Provenance trail (iterations 0–13, including the ~7 failed direct square
attempts and why BR's corner-unification is the only route):
`analysis/drive-agda-sketch-vv/port/REFINEMENT-LOG.md`.

Local toolchain: Agda 2.8.0 + agda/cubical @ 7b9019b (see CI for the pinned setup).
