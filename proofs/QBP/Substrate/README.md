# QBP.Substrate — RESERVED (no Lean files yet)

Condensed/locale mathematics enriching the tower. Napkin-level as of 2026-06:
no Lean files are created here until the first real theorem exists — sorried
scaffolding is banned (the #471 lesson).

Layer rules: `docs/foundations/layer-architecture.md`. When work begins here,
Substrate imports `QBP.Foundations` (enrichment direction — the import DAG runs
opposite to the conceptual onion) and never imports `QBP.Physics` except via
ratified bridge files.

Tracking: the condensed-substrate housekeeping issue (gated "once the tower is
built").
